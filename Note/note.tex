%!TEX program = xelatex
%!TEX options=--shell-escape
\documentclass[12pt]{article}

%
\usepackage[scheme=plain]{ctex}
%
\usepackage{fontspec}
%
\usepackage[margin = 1in]{geometry}

%
\usepackage[dvipsnames]{xcolor}
\usepackage[many]{tcolorbox}

%
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{amsthm}
%
\usepackage{tensor}
%
\usepackage{slashed}
\usepackage{physics}
\usepackage{simpler-wick}

%
\usepackage{mathtools}

%
\usepackage{bm}
\newcommand{\dbar}{\dif\hspace*{-0.18em}\bar{}\hspace*{0.2em}}
\DeclareMathAlphabet\mathbfcal{OMS}{cmsy}{b}{n}
%\usepackage{bbold}
\newcommand*{\dif}{\mathop{}\!\mathrm{d}}
\newcommand*{\euler}{\mathrm{e}}
\newcommand*{\imagi}{\mathrm{i}}

\renewcommand{\vec}[1]{\boldsymbol{\mathbf{#1}}}

\usepackage{caption}
\usepackage{multirow}
\usepackage{enumitem}
\usepackage{booktabs}

%
\usepackage{mathrsfs}
\usepackage{dsfont}

%
\usepackage{hyperref}
\hypersetup{
    colorlinks=true,
    linkcolor=violet,
    filecolor=blue,
    urlcolor=blue,
    citecolor=cyan,
}

%
\usepackage{graphicx}
\usepackage{xkeyval} % 載入用來解析 key-value 參數的套件

% ==========================================
% 自動偵測圖片巨集 (動態讀取 width 終極版)
% ==========================================
\makeatletter
\newlength{\placeholderwidth}
% 定義一個用來捕捉 width 參數的指令
\define@key{placeholder}{width}{\setlength{\placeholderwidth}{#1}}

\let\oldincludegraphics\includegraphics 

\renewcommand{\includegraphics}[2][]{%
  \IfFileExists{../figures/#2}{% 
    \oldincludegraphics[#1]{#2}% 真實圖片
  }{%
    % 1. 先設定一個保底的預設寬度（以防你忘記寫 width=...）
    \setlength{\placeholderwidth}{0.3\textwidth}%
    
    % 2. 動態解析傳入的引數 (#1)
    % 使用 \setkeys* (有星號) 可以安全地忽略我們不認識的參數 (例如 height 等)
    \setkeys*{placeholder}{#1}%
    
    % 3. 精準校正：扣除 \framebox 預設的內邊距與框線厚度
    % 這樣能確保佔位框的「總寬度」完完全全等於你指定的 width
    \addtolength{\placeholderwidth}{-2\fboxsep}%
    \addtolength{\placeholderwidth}{-2\fboxrule}%
    
    % 4. 畫出動態寬度的佔位框
    \framebox{%
      \parbox{\placeholderwidth}{%
        \centering\vspace{1.5cm}
        \tiny\ttfamily\detokenize{#2}\\ 
        not found
        \vspace{1.5cm}%
      }%
    }%
  }%
}
\makeatother
% ==========================================
% ==========================================
\usepackage{subfig}
%
\graphicspath{{../figures/}}

% \usepackage{tikz}
% \usetikzlibrary{positioning}
% \usetikzlibrary{calc}

\usepackage{tikz}
\usetikzlibrary{shapes.geometric, arrows.meta, positioning, fit, calc, backgrounds}

\usepackage{listings}
\usepackage{lstautogobble}
\lstset{
    basicstyle=\ttfamily,
    columns=fullflexible,
    autogobble=true,
}

%
\usepackage{indentfirst}
%
\setlength{\parindent}{2em}
\linespread{1.25}


%
% \setmainfont{Times New Roman}

\title{Note}
\author{Feng-Yang Hsieh}
\date{}

\begin{document}
\maketitle


\section{ATLAS latest study}% (fold)
\label{sec:atlas_latest_study}

    The couplings of longitudinally polarized $W$ and $Z$ bosons to the Higgs boson prevent the divergence of tree-level Vector Boson Scattering (VBS) amplitudes at high energies when the two outgoing bosons are longitudinally polarized, restoring unitarity at the TeV scale. Thus, the VBS processes with longitudinal vector bosons provide a sensitive test of the electroweak (EW) symmetry-breaking mechanism.

    The most recent ATLAS study is detailed in Ref.~\cite{ATLAS:2025wuw}. This study focuses on VBS. Specifically, it investigates the EW production of same-sign $W$ boson pairs (EW $W^{\pm} W^{\pm} jj$), a process to which VBS provides a significant contribution. The key concepts and event selection criteria of this work are summarized below.

    The object selection criteria are as follows:
    \begin{itemize}
        \item \textbf{Electrons:} Must have transverse momentum $p_{\text{T}} > 27~\mathrm{GeV}$ and pseudorapidity $\abs{\eta} < 2.47$, excluding the calorimeter transition region of $1.37 < \abs{\eta} < 1.52$.
        \item \textbf{Muons:} Must have $p_{\text{T}} > 27~\mathrm{GeV}$ and $\abs{\eta} < 2.5$.
        \item \textbf{Jets:} At least two jets with $\abs{\eta} < 4.5$ are required in each event. The leading jet must have $p_{\text{T}} > 65~\mathrm{GeV}$, and the subleading jet must have $p_{\text{T}} > 35~\mathrm{GeV}$.
    \end{itemize}

    The Signal Region (SR) event selection requires:
    \begin{itemize}
        \item A same-sign lepton pair with an invariant mass $m_{\ell\ell} > 20~\mathrm{GeV}$.
        \item Missing transverse momentum magnitude, $E_{\text{T}}^{\text{miss}}$, greater than $30~\mathrm{GeV}$.
        \item The two highest-$p_{\text{T}}$ jets must satisfy a dijet invariant mass $m_{jj} > 500~\mathrm{GeV}$ with an absolute rapidity difference $\abs{\Delta y_{jj}} > 2.0$.
    \end{itemize}
% section atlas_latest_study (end)

\section{Monte Carlo simulation}% (fold)
\label{sec:monte_carlo_simulation}

    \subsection{Sample Generation}% (fold)
    \label{sub:sample_generation}
        The signal processes are the electroweak (EW) production of $W^{\pm}W^{\pm}jj$ in various polarization states. We simulate the EW same-sign $W$ boson pair production at a center-of-mass energy of $\sqrt{s} = 13~\text{TeV}$. The events are generated using \verb|MadGraph 3.3.1|~\cite{Alwall:2014hca}. Both $W$ bosons must decay leptonically. Parton showering and hadronization are simulated with \verb|Pythia 8.306|~\cite{Sjostrand:2014zea}, followed by a fast detector simulation using \verb|Delphes 3.4.2|~\cite{deFavereau:2013fsa}. Jets are reconstructed via the anti-$k_t$ algorithm~\cite{Cacciari:2008gp} with a distance parameter of $R = 0.4$, implemented in \verb|FastJet 3.3.2|~\cite{Cacciari:2011ma}. Reconstructed jets are required to have a transverse momentum of $p_{\text{T}} > 25~\text{GeV}$.

        The following \verb|MadGraph| scripts are used to generate the Monte Carlo (MC) samples for these polarized processes:

        \paragraph{Longitudinal mode: $pp \to W^{\pm}_{\text{L}}W^{\pm}_{\text{L}} jj$}
        \begin{lstlisting}
            generate p p > j j w+{0} w+{0} QCD=0, w+ > l+ vl
            add process p p > j j w-{0} w-{0} QCD=0, w- > l- vl~
            output EW_WWjj_LL
            launch EW_WWjj_LL

            shower=Pythia8
            detector=Delphes
            analysis=OFF
            madspin=OFF
            done

            Cards/delphes_card.dat

            set run_card nevents 10000
            set run_card ebeam1 6500.0
            set run_card ebeam2 6500.0

            set run_card ptj 34.0
            set run_card ptl 26.0
            set run_card misset 29.0

            set run_card etaj 4.6
            set run_card etal 2.6

            set run_card mmjj 480
            set run_card deltaeta 1.9

            set run_card ptj1min 64
            set run_card ptj2min 34

            set run_card use_syst False

            done
        \end{lstlisting}

        \paragraph{Transverse mode: $pp \to W^{\pm}_{\text{T}}W^{\pm}_{\text{T}} jj$}
        \begin{lstlisting}
            generate p p > j j w+{T} w+{T} QCD=0, w+ > l+ vl
            add process p p > j j w-{T} w-{T} QCD=0, w- > l- vl~
            ...
        \end{lstlisting}

        \paragraph{Mixed mode: $pp \to W^{\pm}_{\text{L}}W^{\pm}_{\text{T}} jj$}
        \begin{lstlisting}
            generate p p > j j w+{0} w+{T} QCD=0, w+ > l+ vl
            add process p p > j j w-{0} w-{T} QCD=0, w- > l- vl~
            ...
        \end{lstlisting}

    % subsection sample_generation (end)

    \subsection{Event Selection}% (fold)
    \label{sub:event_selection}

        The event selection criteria applied after the \verb|Delphes| simulation are defined as follows:
        \begin{itemize}
            \item \textbf{Lepton cut:} The invariant mass of the same-sign lepton pair, $m_{\ell\ell}$, must be greater than $20~\mathrm{GeV}$.
            \item \textbf{MET cut:} The missing transverse momentum magnitude, $E_{\text{T}}^{\text{miss}}$, must be greater than $30~\mathrm{GeV}$.
            \item \textbf{Jet cut:} The two highest-$p_{\text{T}}$ tagging jets must satisfy a dijet invariant mass of $m_{jj} > 500~\mathrm{GeV}$ with an absolute rapidity difference of $\abs{\Delta y_{jj}} > 2.0$.
        \end{itemize}

        Table~\ref{tab:EW_WWjj_cutflow_number} summarizes the cutflow (number of expected events and the corresponding selection efficiencies) at each selection stage. The reconstruction requirements for electrons, muons, and jets are identical to those described in Section~\ref{sec:atlas_latest_study}.

        \begin{table}[htpb]
            \centering
            \caption{Number of events and selection efficiencies (pass rates) for EW $W^{\pm}W^{\pm}jj$ production at different selection stages.}
            \label{tab:EW_WWjj_cutflow_number}
            \begin{tabular}{l|rr|rr|rr}
                Cut        & LL     & pass rate & LT     & pass rate & TT     & pass rate \\ \hline
                Total      & 100000 & 1.00      & 100000 & 1.00      & 100000 & 1.00      \\
                Lepton cut & 26777  & 0.27      & 27270  & 0.27      & 29825  & 0.30      \\
                MET cut    & 24781  & 0.25      & 25518  & 0.26      & 28323  & 0.28      \\
                Jet cut    & 20005  & 0.20      & 21087  & 0.21      & 23298  & 0.23      \\
            \end{tabular}
        \end{table}

        Figure~\ref{fig:kinematic_distributions} displays the kinematic distributions for the various polarization states in the SR. Specifically, we present the absolute pseudorapidity difference between the leading and subleading leptons $\abs{\Delta\eta_{\ell\ell}}$, the transverse mass of the dilepton and $E_{\text{T}}^{\text{miss}}$ system $m_{\text{T}}$, and the azimuthal angle difference between the leading and subleading jets $\Delta\phi_{jj}$. The ratios of the individual process contributions to the total signal are shown in the bottom panels. For these combined distributions, we assume that the $W^{\pm}_{\text{L}}W^{\pm}_{\text{L}} jj$, $W^{\pm}_{\text{L}}W^{\pm}_{\text{T}} jj$, and $W^{\pm}_{\text{T}}W^{\pm}_{\text{T}} jj$ states contribute 10\%, 30\%, and 60\% to the total signal, respectively.

        \begin{figure}[htpb]
            \centering
            \subfloat[$\abs{\Delta\eta_{\ell\ell}}$ distribution]{
                \includegraphics[width=0.31\textwidth]{EW_WWjj_deta_ll.pdf}
            }
            \subfloat[$m_{\text{T}}$ distribution]{
                \includegraphics[width=0.31\textwidth]{EW_WWjj_mT.pdf}
            }
            \subfloat[$\Delta\phi_{jj}$ distribution]{
                \includegraphics[width=0.31\textwidth]{EW_WWjj_dphi_jj.pdf}
            }
            \caption{Kinematic distributions of $\abs{\Delta\eta_{\ell\ell}}$, $m_{\text{T}}$, and $\Delta\phi_{jj}$ in the signal region. The ratios of the contributions from different polarization states are shown in the bottom panels.}
            \label{fig:kinematic_distributions}
        \end{figure}

    % subsection event_selection (end)

    \subsection{Absolute Cross-Sections}% (fold)
    \label{sub:absolute_cross_sections}

        Table~\ref{tab:EW_WWjj_abs_cross_section} summarizes the absolute cross-sections for the EW $W^{\pm}W^{\pm}jj$ production across different selection stages. To validate our simulation framework, the final cross-sections after the jet cut are compared with the reference results from a previous thesis~\cite{stange2025longitudinal}.

        \begin{table}[htpb]
            \centering
            \caption{Absolute cross-sections (in fb) for the EW $W^{\pm}W^{\pm}jj$ production at various selection stages. The reference values from the previous thesis and the relative fractions are provided at the final jet cut stage for comparison.}
            \label{tab:EW_WWjj_abs_cross_section}
            \begin{tabular}{l|rrrr}
                \hline
                Cut & LL & LT & TT & Total \\ \hline
                Total            & 0.443 & 2.514 & 3.824 & 6.781 \\
                Lepton cut       & 0.119 & 0.685 & 1.140 & 1.945 \\
                MET cut          & 0.110 & 0.641 & 1.083 & 1.834 \\
                Jet cut          & 0.089 & 0.530 & 0.891 & 1.510 \\ \hline
                Jet cut (Thesis) & 0.177 & 1.030 & 1.718 & 2.925 \\ \hline
                Fraction         & 5.9\% & 35.1\% & 59.0\% & 100\% \\
                Fraction (Thesis)& 6.1\% & 35.2\% & 58.7\% & 100\% \\ \hline
            \end{tabular}
        \end{table}

        As observed in Table~\ref{tab:EW_WWjj_abs_cross_section}, while the relative fractions of the polarization states are in excellent agreement with the reference thesis, the absolute cross-sections in our simulation are approximately a factor of two lower. Further investigation is needed.
    % subsection absolute_cross_sections (end)

    \subsection{\texorpdfstring{$WW$}{WW} Center-of-Mass Reference Frame}% (fold)
    \label{sub:ww_center_of_mass_reference_frame}

        To properly study the scattering dynamics, we modify our \verb|MadGraph| scripts to define the $W$ boson polarizations in the $W^{\pm}W^{\pm}$ center-of-mass (CM) frame.

        The following generation commands are used to produce the MC samples with this specific reference frame definition:

        \paragraph{Longitudinal mode: $pp \to W^{\pm}_{\text{L}}W^{\pm}_{\text{L}} jj$}
        \begin{lstlisting}
            generate p p > w+{0} w+{0} j j QCD=0, w+ > l+ vl
            add process p p > w-{0} w-{0} j j QCD=0, w- > l- vl~
            output MG5/EW_WWjj_LL-WW_cmf
            launch MG5/EW_WWjj_LL-WW_cmf

            shower=Pythia8
            detector=Delphes
            analysis=OFF
            madspin=OFF
            done

            Cards/delphes_card.dat

            set run_card nevents 10000
            set run_card ebeam1 6500.0
            set run_card ebeam2 6500.0

            set run_card ptj 34.0
            set run_card etaj 4.6
            set run_card mmjj 480
            set run_card deltaeta 1.9

            set run_card etal 2.6
            set run_card ptl 26.0
            set run_card misset 29.0

            set run_card ptj1min 64
            set run_card ptj2min 34

            set run_card me_frame 3, 4, 5, 6

            set run_card use_syst False

            done
        \end{lstlisting}

        \paragraph{Transverse mode: $pp \to W^{\pm}_{\text{T}}W^{\pm}_{\text{T}} jj$}
        \begin{lstlisting}
            generate p p > w+{T} w+{T} j j QCD=0, w+ > l+ vl
            add process p p > w-{T} w-{T} j j QCD=0, w- > l- vl~
            ...
        \end{lstlisting}

        \paragraph{Mixed mode: $pp \to W^{\pm}_{\text{L}}W^{\pm}_{\text{T}} jj$}
        \begin{lstlisting}
            generate p p > w+{0} w+{T} j j QCD=0, w+ > l+ vl
            add process p p > w-{0} w-{T} j j QCD=0, w- > l- vl~
            ...
        \end{lstlisting}

        The key modification lies in the \verb|me_frame| setting. More details can be found in ref.~\cite{BuarqueFranzosi:2019boy}. It is crucial to note that when utilizing the in-line decay syntax, \verb|MadGraph| expands the decay products directly into the final-state particle list. Consequently, the indices must be specified as \verb|3, 4, 5, 6| to encompass all the decay products (leptons and neutrinos) required to reconstruct the $WW$ system.

        The remaining shower, detector, and run card settings for the transverse and mixed modes are identical to those of the longitudinal mode and are omitted here for brevity.

        Table~\ref{tab:EW_WWjj_abs_cross_section_ww_cmf} summarizes the absolute cross-sections for the EW $W^{\pm}W^{\pm}jj$ production when the polarizations are defined in the $W^{\pm}W^{\pm}$ CM frame. The absolute cross-sections are still a factor of two lower.
        \begin{table}[htpb]
            \centering
            \caption{Absolute cross-sections for the EW $W^{\pm}W^{\pm}jj$ production at various selection stages, with $W$ boson polarizations defined in the $W^{\pm}W^{\pm}$ center-of-mass frame. Reference values from the previous thesis and the relative fractions are provided at the final jet-cut stage for comparison.}
            \label{tab:EW_WWjj_abs_cross_section_ww_cmf}
            \begin{tabular}{l|rrrr}
                \hline
                Cut & LL [fb] & LT [fb] & TT [fb] & Total [fb] \\ \hline
                Total            & 0.677 & 2.190  & 3.889  & 6.756 \\
                Lepton cut       & 0.190 & 0.577  & 1.143  & 1.910 \\
                MET cut          & 0.177 & 0.535  & 1.081  & 1.793 \\
                Jet cut          & 0.147 & 0.443  & 0.895  & 1.485 \\ \hline
                Jet cut (Thesis) & 0.277 & 0.874  & 1.744  & 2.869 \\ \hline
                Fraction         & 9.4\% & 30.4\% & 60.2\% & 100\% \\
                Fraction (Thesis)& 9.6\% & 30.6\% & 59.8\% & 100\% \\ \hline
            \end{tabular}
        \end{table}

        Figure~\ref{fig:kinematic_distributions_ww_cmf} displays the kinematic distributions for the various polarization states in the SR within the $WW$ center-of-mass frame. These results are consistent with Figure~2 in ref.~\cite{ATLAS:2025wuw}.
        \begin{figure}[htpb]
            \centering
            \subfloat[$\abs{\Delta\eta_{\ell\ell}}$ distribution]{
                \includegraphics[width=0.31\textwidth]{EW_WWjj_deta_ll-ww_cmf.pdf}
            }
            \subfloat[$m_{\text{T}}$ distribution]{
                \includegraphics[width=0.31\textwidth]{EW_WWjj_mT-ww_cmf.pdf}
            }
            \subfloat[$\Delta\phi_{jj}$ distribution]{
                \includegraphics[width=0.31\textwidth]{EW_WWjj_dphi_jj-ww_cmf.pdf}
            }
            \caption{Kinematic distributions of $\abs{\Delta\eta_{\ell\ell}}$, $m_{\text{T}}$, and $\Delta\phi_{jj}$ in the signal region. The ratios of the contributions from different polarization states are shown in the bottom panels.}
            \label{fig:kinematic_distributions_ww_cmf}
        \end{figure}

    % subsection ww_center_of_mass_reference_frame (end)

    \subsection{Simulation with Sherpa}% (fold)
    \label{sub:simulation_with_sherpa}

        To investigate the cross-section discrepancy, we employ a different Monte Carlo generator, \verb|Sherpa 3|~\cite{Sherpa:2024mfk}, which is also the primary generator utilized in the ATLAS analysis.

        We simulate the EW same-sign $W$ boson pair production at a center-of-mass energy of $\sqrt{s} = 13$ TeV. Both $W$ bosons must decay leptonically. Parton showering and hadronization are handled internally by \verb|Sherpa|, followed by a fast detector simulation using \verb|Delphes 3.4.2|~\cite{deFavereau:2013fsa}. Jets are reconstructed via the anti-$k_t$ algorithm~\cite{Cacciari:2008gp} with a distance parameter of $R = 0.4$, implemented in \verb|FastJet 3.3.2|~\cite{Cacciari:2011ma}. The reconstructed jets are required to have a transverse momentum of $p_{\text{T}} > 25$ GeV.

        Unlike \verb|MadGraph|, which can generate pure polarized samples, \verb|Sherpa| generates events and assigns polarization weights to each of them. These weights represent the probability of an event belonging to a specific polarization state. Due to technical constraints within the generator, the $W^{+}W^{+}jj$ and $W^{-}W^{-}jj$ processes must be generated as separate samples.

        The following YAML configuration snippet illustrates the generation of the $W^{+}W^{+}jj$ sample using \verb|Sherpa|:
        \paragraph{$W^{+}W^{+} jj$ sample}
        \begin{lstlisting}
            EVENTS: 100
            RANDOM_SEED: 1

            BEAMS: 2212
            BEAM_ENERGIES: 6500

            PDF_SET: NNPDF30_nlo_as_0118

            ME_GENERATORS:
              - Comix
            COMIX_DEFAULT_GAUGE: 0

            # scales
            MEPS:
              CORE_SCALE: VAR{0.5*Abs2(p[2]+p[3])}

            # physical constants and width settings
            PARTICLE_DATA:
              24: {Width: 0}   # W Boson
              23: {Width: 0}   # Z Boson
            WIDTH_SCHEME: Fixed

            # decay and polarization settings
            HARD_DECAYS:
              Enabled: true
              Channels:
                24,12,-11: {Status: 2}   # W+ -> nu_e e+
                24,14,-13: {Status: 2}   # W+ -> nu_mu mu+
              Pol_Cross_Section:
                Enabled: true
                Reference_System: [Lab, COM]

            # process definition: same-sign W boson pair production with two jets
            PROCESSES:
              - 93 93 -> 24 24 93 93:
                  Order: {QCD: 0, EW: 4}

            # final state particles cuts
            SELECTORS:
              - [PT, 93, 34, 10000]
              - [Eta, 93, -4.6, 4.6]
              - [Mass, 93, 93, 480, 10000]
              - [DY, 93, 93, 1.9, 10000]

              - VariableSelector:
                  Variable: PT
                  Flavs: 93
                  Ranges:
                    - [60.0, 10000.0]
                  Ordering: [PT_UP]

            # output settings
            EVENT_OUTPUT: HepMC3_GenEvent[EW_WpWpjj.hepmc]
        \end{lstlisting}

        \paragraph{$W^{-}W^{-} jj$ sample}
        \begin{lstlisting}
            EVENTS: 100
            RANDOM_SEED: 1

            BEAMS: 2212
            BEAM_ENERGIES: 6500

            PDF_SET: NNPDF30_nlo_as_0118

            ME_GENERATORS:
              - Comix
            COMIX_DEFAULT_GAUGE: 0

            # scales
            MEPS:
              CORE_SCALE: VAR{0.5*Abs2(p[2]+p[3])}

            # physical constants and width settings
            PARTICLE_DATA:
              24: {Width: 0}   # W Boson
              23: {Width: 0}   # Z Boson
            WIDTH_SCHEME: Fixed

            # decay and polarization settings
            HARD_DECAYS:
              Enabled: true
              Channels:
                -24,-12,11: {Status: 2}   # W- -> nubar e-
                -24,-14,13: {Status: 2}   # W- -> nubar mu-
              Pol_Cross_Section:
                Enabled: true
                Reference_System: [Lab, COM]

            # process definition: same-sign W boson pair production with two jets
            PROCESSES:
              - 93 93 -> -24 -24 93 93:
                  Order: {QCD: 0, EW: 4}

            # final state particles cuts
            SELECTORS:

              - [PT, 93, 34, 10000]
              - [Eta, 93, -4.6, 4.6]
              - [Mass, 93, 93, 480, 10000]
              - [DY, 93, 93, 1.9, 10000]

              - VariableSelector:
                  Variable: PT
                  Flavs: 93
                  Ranges:
                    - [60.0, 10000.0]
                  Ordering: [PT_UP]

            # output settings
            EVENT_OUTPUT: HepMC3_GenEvent[EW_WmWmjj.hepmc]
        \end{lstlisting}

        It is important to note that the decay channel specification in \verb|Sherpa| is strictly sensitive. The particle ordering must match the internal definitions precisely; for instance, \verb|24,12,-11: {Status: 2}| is valid, whereas \verb|24,-11,12: {Status: 2}| will not be recognized.

        Table~\ref{tab:sherpa_WpWm_cross_section} details the absolute cross-sections for the individual $W^{+}W^{+}jj$ and $W^{-}W^{-}jj$ samples at various selection stages, with the polarization states defined in the $W^{\pm}W^{\pm}$ center-of-mass frame.
        \begin{table}[htpb]
            \centering
            \caption{Absolute cross-sections for the separate $W^{+}W^{+}jj$ and $W^{-}W^{-}jj$ samples at various selection stages simulated with \texttt{Sherpa}. Polarization states are defined in the $W^{\pm}W^{\pm}$ center-of-mass frame.}
            \label{tab:sherpa_WpWm_cross_section}
            \begin{tabular}{l|rrrr|rrrr}
                           & \multicolumn{4}{c|}{$W^{+}W^{+}jj$ [fb]} & \multicolumn{4}{c}{$W^{-}W^{-}jj$ [fb]}\\
                Stage      & LL       & LT       & TT       & Total   & LL       & LT       & TT      & Total  \\ \hline
                Total      & 0.527    & 1.633    & 2.949    & 5.108   & 0.162    & 0.475    & 0.850   & 1.487  \\
                Lepton cut & 0.153    & 0.441    & 0.863    & 1.457   & 0.048    & 0.143    & 0.295   & 0.486  \\
                MET cut    & 0.132    & 0.394    & 0.776    & 1.302   & 0.040    & 0.122    & 0.250   & 0.412  \\
                Jet cut    & 0.106    & 0.319    & 0.633    & 1.057   & 0.030    & 0.094    & 0.199   & 0.323  \\
            \end{tabular}
        \end{table}

        The combined absolute cross-sections for the EW $W^{\pm}W^{\pm}jj$ production are summarized in table~\ref{tab:sherpa_WWjj_cross_section}. While the relative fractions of the polarization states remain in excellent agreement with the reference thesis, the combined absolute cross-section generated by \verb|Sherpa| ($1.380~\text{fb}$ at the jet cut stage) is still approximately a factor of two lower than the reference value ($2.933~\text{fb}$).
        \begin{table}[htpb]
            \centering
            \caption{Combined absolute cross-sections for the EW $W^{\pm}W^{\pm}jj$ production simulated with \texttt{Sherpa}. Reference values from the previous thesis and the relative fractions are provided at the final jet-cut stage for comparison.}
            \label{tab:sherpa_WWjj_cross_section}
            \begin{tabular}{l|rrrr}
                Cut               & LL [fb] & LT [fb] & TT [fb] & Total [fb] \\ \hline
                Total             & 0.688   & 2.108   & 3.799   & 6.595      \\
                Lepton cut        & 0.202   & 0.583   & 1.158   & 1.943      \\
                MET cut           & 0.172   & 0.516   & 1.026   & 1.714      \\
                Jet cut           & 0.136   & 0.413   & 0.831   & 1.380      \\ \hline
                Jet cut (Thesis)  & 0.285   & 0.909   & 1.778   & 2.933      \\ \hline
                Fraction          & 9.9\%   & 29.9\%  & 60.2\%  & 100\%      \\
                Fraction (Thesis) & 9.6\%   & 30.6\%  & 59.8\%  & 100\%
            \end{tabular}
        \end{table}

    % subsection simulation_with_sherpa (end)

    \subsection{Particle-Level Selection}% (fold)
    \label{sub:particle_level_selection}

        Upon a detailed review of the reference thesis~\cite{stange2025longitudinal}, we identified that their reported yields and cross-sections are evaluated at the particle level rather than the reconstructed level. To perform a fair comparison, we modified our event selection framework to operate on particle-level objects.

        Specifically, the variables accessed in the \verb|Delphes| analysis were updated as follows:
        \begin{itemize}
            \item \textbf{Leptons:} Reconstructed leptons (\verb|Electron| and \verb|Muon| collections) are replaced by generator-level stable final-state leptons from the \verb|Particle| branch.
            \item \textbf{Missing Transverse Momentum:} The reconstructed missing transverse energy (\verb|MissingET|) is replaced by the true missing transverse momentum (\verb|GenMissingET|).
            \item \textbf{Jets:} Reconstructed jets (\verb|Jet|) are replaced by truth-level jets (\verb|GenJet|).
        \end{itemize}

        Furthermore, the kinematic selection criteria at the particle level are different from the reconstructed-level SR selection defined in Section~\ref{sub:event_selection}. Based on the reference thesis, the particle-level selection is defined as follows:
        \begin{itemize}
            \item \textbf{Leptons:} Exactly two same-sign leptons with $p_{\text{T}} > 27~\mathrm{GeV}$. Muons must satisfy $\abs{\eta} < 2.5$. Electrons must satisfy $\abs{\eta} < 2.47$, excluding the transition region $1.37 \leq \abs{\eta} \leq 1.52$, with an additional restriction of $\abs{\eta} < 1.37$ for the $ee$ channel.

            \item \textbf{Dilepton Mass \& $Z$-veto:} The dilepton invariant mass must be $m_{\ell\ell} > 20~\mathrm{GeV}$. Additionally, a $Z$-boson veto is applied to the $ee$ channel, requiring $\abs{m_{ee} - m_{Z}} > 15~\mathrm{GeV}$ to suppress background contributions.

            \item \textbf{Missing Transverse Momentum:} $E_{\text{T}}^{\text{miss}} \geq 30~\mathrm{GeV}$.

            \item \textbf{Jets:} At least two jets are required. The leading and subleading jets must satisfy $p_{\text{T}} > 65~\mathrm{GeV}$ and $p_{\text{T}} > 35~\mathrm{GeV}$, respectively. The dijet invariant mass threshold is relaxed to $m_{jj} \geq 200~\mathrm{GeV}$, and the rapidity difference must be $\Delta y_{jj} > 2$.
        \end{itemize}

        The PDF sets are also updated to match the reference thesis~\cite{stange2025longitudinal}. We use the \verb|NNPDF23_nlo_as_0119| PDF set~\cite{Ball:2012cx} for \verb|MadGraph| and \verb|PDF4LHC21_40_pdfas| PDF set~\cite{PDF4LHCWorkingGroup:2022cjn}for \verb|Sherpa|.

        The updated absolute cross-sections for the EW $W^{\pm}W^{\pm}jj$ production at the particle level are summarized in Table~\ref{tab:combined_WWjj_cross_section_particle_level}. Although the relative fractions of the polarization states remain in excellent agreement with the reference thesis, a systematic normalization discrepancy still exists in the absolute cross-sections.

        \begin{table}[htpb]
            \centering
            \caption{Combined absolute cross-sections for the EW $W^{\pm}W^{\pm}jj$ production simulated with \texttt{MadGraph} and \texttt{Sherpa} using \textbf{particle-level selection}. Reference values from the previous thesis and the relative fractions are provided at the final jet-cut stage for comparison.}
            \label{tab:combined_WWjj_cross_section_particle_level}
            \begin{tabular}{l|rrrr|rrrr}
                \hline
                Cut               & \multicolumn{4}{c|}{MadGraph [fb]} & \multicolumn{4}{c}{Sherpa [fb]}  \\
                                  & LL      & LT      & TT     & Total & LL     & LT     & TT     & Total \\ \hline
                Total             & 0.383   & 1.188   & 2.185  & 3.755 & 0.729  & 2.252  & 3.989  & 6.970 \\
                Lepton cut        & 0.267   & 0.850   & 1.665  & 2.782 & 0.291  & 0.906  & 1.746  & 2.943 \\
                MET cut           & 0.260   & 0.834   & 1.636  & 2.731 & 0.240  & 0.786  & 1.522  & 2.548 \\
                Jet cut           & 0.205   & 0.623   & 1.129  & 1.957 & 0.188  & 0.569  & 1.021  & 1.777 \\ \hline
                Jet cut (Thesis)  & 0.277   & 0.874   & 1.744  & 2.895 & 0.285  & 0.909  & 1.778  & 2.972 \\ \hline
                Fraction          & 10.5\%  & 31.9\%  & 57.7\% & 100\% & 10.6\% & 32.0\% & 57.5\% & 100\% \\
                Fraction (Thesis) & 9.6\%   & 30.2\%  & 60.2\% & 100\% & 9.6\%  & 30.6\% & 59.8\% & 100\% \\ \hline
            \end{tabular}
        \end{table}

        The thesis also mentioned the overlapping removal strategies for the jet and lepton objects. However, it is not clear if they applied the overlapping removal and what kind of strategy they considered.

    % subsection particle_level_selection (end)

    \subsection{Background Processes}% (fold)
\label{sub:background_processes}

    In this section, we focus on the primary background sources: the irreducible QCD $W^\pm W^\pm jj$ production and the reducible $W^{\pm}Zjj$ process.

        \subsubsection{QCD \texorpdfstring{$W^{\pm}W^{\pm}jj$}{WWjj} Production}% (fold)
        \label{subs:qcd_wwjj_production}

            The QCD-induced same-sign $W$ boson pair production in association with two jets is an irreducible background, as its final state is identical to that of the signal. At tree level, this process is of order $\mathcal{O}(\alpha_S^2 \alpha^4)$.

            The following \verb|MadGraph| scripts are used to generate the MC samples:
            \begin{lstlisting}
                generate p p > w+ w+ j j QCD<=2 QED<=2, w+ > l+ vl
                add process p p > w- w- j j QCD<=2 QED<=2, w- > l- vl~

                ...
            \end{lstlisting}

        % subsubsection qcd_wwjj_production (end)

        \subsubsection{INT \texorpdfstring{$W^{\pm}W^{\pm}jj$}{WWjj} Production}% (fold)
        \label{subs:int_wwjj_production}

            The interference (INT) $W^{\pm}W^{\pm}jj$ background arises from the quantum interference between the purely electroweak and the QCD-induced amplitudes. At tree level, this process is of order $\mathcal{O}(\alpha_S \alpha^5)$.

        % subsubsection int_wwjj_production (end)

        \subsubsection{\texorpdfstring{$W^{\pm}Zjj$}{WZjj} Production}% (fold)
        \label{subs:wzjj_production}

            The $WZ$ production in association with jets, where $WZ \to \ell\nu\ell\ell$, is the most prominent reducible background. It mimics the same-sign $WW$ topology when one of the leptons from the $Z$ boson decay is ``lost'' (e.g., falls outside the detector acceptance or fails identification criteria).

            The following \verb|MadGraph| scripts are used to generate the MC samples:
            \begin{lstlisting}
                generate p p > w+ z j j QCD=0, w+ > l+ vl, z > l+ l-
                add process p p > w- z j j QCD=0, w- > l- vl~, z > l+ l-

                ...
            \end{lstlisting}

        % subsubsection wzjj_production (end)

        \subsubsection{Non-Prompt Leptons}% (fold)
        \label{subs:non_prompt_lepton}

            Leptons originating from hadron decays and jets misidentified as leptons are referred to as non-prompt leptons. Since this background is estimated using data-driven methods in the experimental analysis, we omit its estimation in our current Monte Carlo simulation framework.

        % subsubsection non_prompt_lepton (end)

        \subsubsection{Full Process vs. Narrow Width Approximation (NWA)}% (fold)
        \label{subs:full_process_and_nwa}

            The most rigorous approach to generating these events is to simulate the full $2 \to 6$ process. For instance, for the EW $W^{\pm}W^{\pm}jj$ signal, the full process command would be:
            \begin{lstlisting}
                generate p p > j j l+ vl l+ vl QCD=0
                add process p p > j j l- vl~ l- vl~ QCD=0
            \end{lstlisting}

            However, simulating the full process requires evaluating a vast number of Feynman diagrams and integrating over a highly complex phase space. This often leads to severe integration inefficiencies and divergences, making it prohibitive to generate a sufficient number of events.

            We investigated the $W^{\pm}Zjj$ case and found that the full simulation and the Narrow Width Approximation (NWA) approach produce highly consistent absolute cross-sections after applying the SR cuts. Therefore, we temporarily adopt the NWA for our current generation setup to ensure efficiency.

        % subsubsection full_process_and_nwa (end)

        \subsubsection{Yields and Comparisons}% (fold)
		\label{subs:yields_and_comparisons}

			To validate our simulation framework, the expected event yields are compared against the reference ATLAS results. Table~\ref{tab:background_yields_final_comparison} summarizes the expected signal and background yields in the SR. The ``ATLAS'' column represents the values reported in Table~3 of Ref.~\cite{ATLAS:2023sua}, the ``Thesis'' columns represent the values reported in Table~9.1 and Table~G.2 of Ref.~\cite{stange2025longitudinal}, while the remaining columns represent our simulation results. Here, we normalize our simulated yields to an integrated luminosity of $\mathcal{L} = 139~\mathrm{fb}^{-1}$.

			\begin{table}[htbp]
				\centering
				\caption{Expected yields in the signal region (SR). The ATLAS results are shown alongside Thesis values and our simulation estimates using different generator versions, assuming an integrated luminosity of $139~\mathrm{fb}^{-1}$.}
				\label{tab:background_yields_final_comparison}
				\begin{tabular}{l|ccc|ccc}
					\hline
					\hline
					Process & ATLAS & Thesis 9.1 & Thesis G.2\footnotemark[1] & MG 3.3.1 & MG 3.7.0 & Sh 3.0.3 \\
					\hline
					$W^{\pm}W^{\pm}jj$ EW  & $235$  & 206.52 & 211.29\footnotemark[2] & 177.98 & 190.28 & 191.18 \\
					$W^{\pm}W^{\pm}jj$ QCD & $24$   & 24.05  & 24.27                  & 14.38  & 12.96  & 19.12  \\
					$W^{\pm}Zjj$ EW        & $14.9$ & 14.95  & 15.07                  & 9.12   & 9.50   & 32.55  \\
					$W^{\pm}Zjj$ QCD       & $83$   & 82.75\footnotemark[3] & 77.0    & 18.47  & 16.30  & 80.15  \\
					\hline
					\hline
				\end{tabular}
			\end{table}

			\footnotetext[1]{Values from the pre-fit event yields (Table G.2), where the three DNN score bins are merged.}
			\footnotetext[2]{The sum of the LL, TL, and TT polarization processes.}
			\footnotetext[3]{The predicted events appear to be scaled down (e.g., $82.75 / 2 \approx 41.38 \to 28.50$). The authors note in the caption that an additional normalization factor was applied to achieve a better agreement with the expected data. However, the exact derivation of this factor is not detailed.}

		% subsubsection yields_and_comparisons (end)

    % subsection background_processes (end)

    \subsection{Determination of the Cross-Section for Sherpa Samples}% (fold)
    \label{sub:what_cross_section_should_be_used_in_sherpa_samples}

        When utilizing \verb|Sherpa| to generate MC samples, there are two primary stages. First, the generator performs a numerical integration over the defined phase space to calculate the total cross-section. Subsequently, it generates events and writes the kinematic information and local cross-section into the resulting \verb|HepMC| file.

        In our previous analyses, the cross-section was estimated by extracting the values recorded event-by-event in the \verb|HepMC| file and computing their average. However, the values written to the event record are local estimates derived during the generating stage. Consequently, they are subject to large statistical fluctuations, which can introduce biases into the final normalization.

        Conversely, the cross-section reported in the \verb|Sherpa| log output that was evaluated during the initial phase-space integration stage is more suitable. Because this integration stage utilizes vast sampling points before generating events, it reduces statistical fluctuations and yields a more stable cross-section.

        Table~\ref{tab:sherpa_xs_comparison} presents a comparison of the absolute cross-sections ($\sigma_{\text{gen}}$) and the corresponding expected yields ($N_{\text{exp}}$) in the SR obtained via these two methods: the integration log output (New) and the averaged \verb|HepMC| weights (Old).

        \begin{table}[htpb]
            \centering
            \caption{Comparison of the generation cross-sections ($\sigma_{\text{gen}}$) and expected yields ($N_{\text{exp}}$) in the SR using the \texttt{Sherpa} log output (New) versus the averaged \texttt{HepMC} values (Old). The percentage difference highlights the consistent overestimation bias introduced by the \texttt{HepMC} method.}
            \label{tab:sherpa_xs_comparison}
            \begin{tabular}{l|rr|rr|r}
                \hline
                \hline
                Sample & $\sigma_{\text{gen}}^{\text{New}}$ [fb] & $\sigma_{\text{gen}}^{\text{Old}}$ [fb] & $N_{\text{exp}}^{\text{New}}$ & $N_{\text{exp}}^{\text{Old}}$ & Diff. \\
                \hline
                $W^{\pm}W^{\pm}jj$ EW  & 7.895  & 8.689  & 178.22 & 196.14 & $-$9.13\% \\
                $W^{\pm}W^{\pm}jj$ QCD & 2.245  & 2.441  & 17.88  & 19.44  & $-$8.01\% \\
                $W^{\pm}Zjj$ EW        & 8.242  & 8.585  & 32.76  & 34.13  & $-$4.00\% \\
                $W^{\pm}Zjj$ QCD       & 20.279 & 21.278 & 72.44  & 76.01  & $-$4.70\% \\
                \hline
                \hline
            \end{tabular}
        \end{table}

        As observed in the table, the \verb|HepMC| average consistently overestimates the expected yields by roughly 4\% to 9\% across various processes. All subsequent analyses will utilize the cross-section obtained from the \verb|Sherpa| generation log.

    % subsection what_cross_section_should_be_used_in_sherpa_samples (end)

    \subsection{MadGraph Run Card Selection Criteria Issues}% (fold)
    \label{sub:madgraph_run_card_selection_criteria_issue}

        To increase the event generation efficiency for the SR phase space, we applied kinematic cuts at the generator level (as detailed in Section~\ref{sub:ww_center_of_mass_reference_frame}). While these cuts are appropriate for generating pure $W^{\pm}W^{\pm}jj$ signal samples, they introduce biases when extending the analysis to Control Regions (CR) or simulating reducible backgrounds such as $W^{\pm}Zjj$.

        For the CR, the analysis investigates the low-$m_{jj}$ region, specifically $200~\mathrm{GeV} < m_{jj} < 500~\mathrm{GeV}$. The previous generator-level cut of \verb|set run_card mmjj 480| is problematic, as it truncates almost all valid events that would populate this CR.

        Furthermore, for the $W^{\pm}Zjj \to \ell\nu\ell\ell$ background, the global lepton transverse momentum cut \verb|set run_card ptl 26.0| causes an underestimation. The \verb|ptl| parameter in \verb|MadGraph| enforces a minimum $p_{\text{T}}$ requirement on \textit{all} generated leptons.

        \textbf{Example:} Consider a $W^+Z \to \ell^+ \nu \ell^+ \ell^-$ event where the two same-sign leptons have $p_{\text{T}} = 45~\mathrm{GeV}$ and $35~\mathrm{GeV}$, while the third lepton ($\ell^-$) is soft with $p_{\text{T}} = 15~\mathrm{GeV}$. In the detector simulation, this soft $\ell^-$ might be ``lost'' (e.g., falling outside the acceptance or failing identification), leaving exactly two same-sign leptons that pass the SR cuts ($p_{\text{T}} > 27~\mathrm{GeV}$). However, because the third lepton's $p_{\text{T}}$ is lower than the $26.0~\mathrm{GeV}$ threshold, \verb|MadGraph| will discard this event at the generation level. Consequently, this topology is completely lost.

        To resolve these issues, the \verb|MadGraph| run card scripts must be updated to use looser, ordered cuts. Taking the QCD $W^{\pm}Zjj$ process as an example:

        \begin{lstlisting}
            generate p p > w+ z j j QCD<=2 QED<=2, w+ > l+ vl, z > l+ l-
            add process p p > w- z j j QCD<=2 QED<=2, w- > l- vl~, z > l+ l-
            output MG5/WZjj_QCD-NWA-v370
            launch MG5/WZjj_QCD-NWA-v370

            shower=Pythia8
            detector=Delphes
            analysis=OFF
            madspin=OFF
            done

            Cards/delphes_card.dat

            set run_card nevents 10000
            set run_card ebeam1 6500.0
            set run_card ebeam2 6500.0

            set run_card pdlabel lhapdf
            set run_card lhaid 260000

            set run_card ptj 20.0
            set run_card etaj 5.0
            set run_card mmjj 150.0
            set run_card deltaeta 1.5
            set run_card ptj1min 50.0
            set run_card ptj2min 25.0

            set run_card etal 2.7
            set run_card ptl1min 20.0
            set run_card ptl2min 20.0

            set run_card misset 10.0

            set run_card use_syst False
            set run_card cut_decays True

            done
        \end{lstlisting}

        The primary changes include relaxing the dijet invariant mass baseline to \verb|mmjj 150.0|, and replacing the global \verb|ptl| cut with ordered lepton cuts: \verb|ptl1min 20.0| and \verb|ptl2min 20.0|. This ensures that only the leading and subleading leptons are constrained, leaving the third lepton unrestricted. Other kinematic variables are relaxed to safely encompass the full reconstructed-level phase space.


        Table~\ref{tab:background_yields_updated} presents the updated event yields in the SR. In this revised comparison, we utilized the cross-sections from the \verb|Sherpa| log outputs (Section~\ref{sub:what_cross_section_should_be_used_in_sherpa_samples}) and applied the relaxed, ordered generator-level cuts for the \verb|MadGraph| 3.7.0 samples to prevent phase-space truncation (Section~\ref{sub:madgraph_run_card_selection_criteria_issue}).
        \begin{table}[htbp]
            \centering
            \caption{Updated expected yields in the SR. The ATLAS results are shown alongside the reference Thesis values and our refined simulation estimates (MG 3.7.0 with relaxed cuts and Sherpa 3.0.3 with log-based cross-sections). All values assume an integrated luminosity of $139~\mathrm{fb}^{-1}$.}
            \label{tab:background_yields_updated}
            \begin{tabular}{l|ccc|cc}
                \hline
                \hline
                Process & ATLAS & Thesis 9.1 & Thesis G.2 & MG 3.7.0 & Sherpa 3.0.3 \\
                \hline
                $W^{\pm}W^{\pm}jj$ EW  & $235$  & 206.52 & 211.29 & 204.08 & 178.22 \\
                $W^{\pm}W^{\pm}jj$ QCD & $24$   & 24.05  & 24.27  & 14.48  & 17.88  \\
                $W^{\pm}Zjj$ EW        & $14.9$ & 14.95  & 15.07  & 22.94  & 32.76  \\
                $W^{\pm}Zjj$ QCD       & $83$   & 82.75  & 77.0   & 50.23  & 72.44  \\
                \hline
                \hline
            \end{tabular}
        \end{table}

    % subsection madgraph_run_card_selection_criteria_issue (end)

% section monte_carlo_simulation (end)

\section{Dense Neural Network}% (fold)
\label{sec:dense_neural_network}

    Because the kinematic relationships between the signal and background processes are complex, a single variable is insufficient for optimal separation. Therefore, multivariate analysis techniques, specifically Deep Neural Networks (DNNs), are employed to extract sensitive features to distinguish signal and background events~\cite{ATLAS:2025wuw}. We consider the Dense Neural Networks (Dense-NNs) as our first trial.

    \subsection{Input Features}% (fold)
    \label{sub:input_features}

        The Dense-NN relies on a set of low-level and high-level kinematic variables. These input features are defined as follows:
        \begin{itemize}
            \item $(p_{\text{T}}^{l}, \eta^{l}, \text{flavor}^{l})$: Transverse momentum, pseudorapidity, and flavor type of the leading and sub-leading leptons.

            \item $(p_{\text{T}}^{j}, \eta^{j})$: Transverse momentum and pseudorapidity of the leading and sub-leading jets.

            \item $E_{\text{T}}^{\text{miss}}$: Missing transverse momentum magnitude.

            \item $\Delta\phi(x, \ell_1)$: Difference in azimuthal angle between the object $x$ and the leading lepton. The object $x$ can be the sub-leading lepton, leading jet, sub-leading jet, or the missing transverse momentum $E_{\text{T}}^{\text{miss}}$.

            \item $Z_{\ell}^{\star}$: Zeppenfeld variables~\cite{Rainwater:1996ud} of the leading and sub-leading leptons. The pseudorapidities of the leptons and tagging jets are used to define this variable:
                \begin{equation}
                    Z_{\ell}^{\star} = \abs{\frac{\eta_\ell - \frac{1}{2} \left( \eta_{j_1} + \eta_{j_2} \right) }{\eta_{j_1} - \eta_{j_2}}}.
                \end{equation}

            \item $m_\text{T}(x, E_{\text{T}}^{\text{miss}})$: Transverse mass of the object $x$ and the $E_{\text{T}}^{\text{miss}}$ system. The object $x$ can be the leading lepton, sub-leading lepton, or the di-lepton system. The standard definition for a two-object system is:
                \begin{equation}
                    m_\text{T}(x, E_{\text{T}}^{\text{miss}}) = \sqrt{\left( E_{\text{T}}^{x} + E_{\text{T}}^{\text{miss} } \right)^2 - \left| \vec{p}_{\text{T}}^{x} + \vec{p}_{\text{T}}^{\text{miss}} \right|^2},
                \end{equation}
                where the scalar transverse energy is evaluated as $E_{\text{T}} = \sqrt{m^2 + |\vec{p}_{\text{T}}|^2}$.

            \item $m_{\text{T}}^{0}$: Projected transverse mass:
                \begin{equation}
                    m_{\text{T}}^{0} = \sqrt{\left( p_{\text{T}}^{\ell_1} + p_{\text{T}}^{\ell_2} + E_{\text{T}}^{\text{miss} } \right)^2 - \left| \vec{p}_{\text{T}}^{\ell_1} + \vec{p}_{\text{T}}^{\ell_2} + \vec{p}_{\text{T}}^{\text{miss}} \right|^2}.
                \end{equation}
                Because the rest mass of an electron or muon is negligible in this high-energy context ($m \approx 0$), the transverse energy of a single lepton simplifies to its transverse momentum ($E_{\text{T}}^{\ell} \approx p_{\text{T}}^{\ell}$). The key difference between the standard di-lepton transverse mass ($m_{\text{T}}$) and the projected transverse mass ($m_{\text{T}}^{0}$) lies in how the scalar energy sum is constructed. The standard $m_{\text{T}}$ treats the two leptons as a single composite system, whereas $m_{\text{T}}^{0}$ treats the two leptons as separate massless entities and directly sums their individual scalar transverse momenta.

            \item $(\Delta R_{\ell\ell}, \Delta\eta_{\ell\ell}, m_{\ell\ell}, p_{\text{T}}^{\ell\ell})$: Angular distance $\Delta R$, pseudorapidity difference, invariant mass, and transverse momentum of the di-lepton system.

            \item $(\Delta R_{jj}, \Delta y_{jj}, m_{jj}, \Delta\phi_{jj})$: Angular distance $\Delta R$, rapidity difference, invariant mass, and azimuthal angle difference of the di-jet system.

            \item $\displaystyle \frac{p_{\text{T}}^{\ell_1} p_{\text{T}}^{\ell_2}}{p_{\text{T}}^{j_1} p_{\text{T}}^{j_2}}$: Ratio of the scalar transverse momenta products of the leptons to the jets.

            \item $\min(\Delta R_{\ell, j})$: Minimum angular distance $\Delta R$ between any of the signal leptons and jets.
        \end{itemize}

    % subsection input_features (end)

    \subsection{Dataset Construction and Validation Strategy}% (fold)
    \label{sub:datasets_construction}

        To ensure the robustness of the neural network models and prevent over-training, a 5-fold cross-validation strategy is employed, which is used in the ATLAS analysis. The total dataset is partitioned into training, validation, and test sets with a fixed ratio of 3:1:1.

        The assignment of each event to a specific subset is determined by its event number. For a given fold index $i_{\text{fold}} \in \{0, 1, 2, 3, 4\}$, the splitting rule is defined as follows:
        \begin{itemize}
            \item \textbf{Test Set}: Events satisfying $(\text{Event Number} - i_{\text{fold}}) \pmod 5 = 0$.
            \item \textbf{Validation Set}: Events satisfying $(\text{Event Number} - i_{\text{fold}} + 1) \pmod 5 = 0$.
            \item \textbf{Training Set}: All remaining events that do not fall into the test or validation sets.
        \end{itemize}

        The training processes are divided into two distinct stages: a primary inclusive network ($\text{DNN}_{W^{\pm}W^{\pm}}$) designed to separate the EW signal from all backgrounds, and secondary polarization-specific networks ($\text{DNN}_{\text{pol}}$) trained exclusively on the EW samples to distinguish specific polarization states.

        Table~\ref{tab:dnn_dataset_composition} details the exact physics processes used for these neural networks and their corresponding class labels (Signal or Background) in the reference thesis~\cite{stange2025longitudinal}. For the polarization networks, the mixed states are grouped such that ``LX'' denotes the sum of LL and TL states, while ``TX'' denotes the sum of TL and TT states.
        \begin{table}[htbp]
            \centering
            \caption{Process usage and assigned labels across the different DNN training stages based on the reference thesis~\cite{stange2025longitudinal}. Labels are denoted as S = Signal, B = Background, and - = Not Used. The LX category represents the sum of the LL and TL states, whereas TX represents the sum of the TL and TT states.}
            \label{tab:dnn_dataset_composition}
            \begin{tabular}{lcccc}
            \toprule
            Physics Process & Gen. Events & $\text{DNN}_{W^{\pm}W^{\pm}}$ & \begin{tabular}{@{}c@{}}$\mathrm{DNN}_{\text{pol}}$ \\ (LL vs TX)\end{tabular} & \begin{tabular}{@{}c@{}}$\mathrm{DNN}_{\text{pol}}$ \\ (LX vs TT)\end{tabular} \\
            \midrule
            $W^{\pm}W^{\pm}jj$-EW LL (WW-cmf)  & 215,008 & - & S & S \\
            $W^{\pm}W^{\pm}jj$-EW TL (WW-cmf)  & 170,387 & - & B & S \\
            $W^{\pm}W^{\pm}jj$-EW TT (WW-cmf)  & 456,456 & - & B & B \\
            $W^{\pm}W^{\pm}jj$-EW LL (pp-cmf)  & 19,673  & - & - & - \\
            $W^{\pm}W^{\pm}jj$-EW TL (pp-cmf)  & 21,471  & - & - & - \\
            $W^{\pm}W^{\pm}jj$-EW TT (pp-cmf)  & 46,320  & - & - & - \\
            $W^{\pm}W^{\pm}jj$-EW (Unpol.)     & 201,488 & S & - & - \\
            $W^{\pm}W^{\pm}jj$-QCD             & 25,375  & B & - & - \\
            $W^{\pm}W^{\pm}jj$-Int.            & 79,805  & B & - & - \\
            $W^{\pm}Z$-EW                      & 8,273   & B & - & - \\
            $W^{\pm}Z$-QCD (Comb.)             & 49,999  & B & - & - \\
            Top quark processes                & 2,192   & B & - & - \\
            $ZZ$ production                    & 2,880   & B & - & - \\
            Charge-flip                        & 8,231   & B & - & - \\
            \bottomrule
            \end{tabular}
        \end{table}

        Each fold is used to train an individual Dense-NN, resulting in an ensemble of five distinct networks. Crucially, this 5-fold application strategy is essential to prevent evaluation bias when predicting the final scores. 

        In the reference thesis~\cite{stange2025longitudinal}, the author states that ``predictions obtained using testing datasets can be assumed to be identically distributed as the predictions on measured data'' and that ``the prediction is calculated using the DNN where the respective events were part of the testing dataset.'' However, the thesis omits a direct procedural description of how to handle the experimental data, which possesses no true labels and never participates in the training, validation, or testing splits. 

        To bridge this methodological gap and ensure a statistically rigorous evaluation, we \textit{infer} that the experimental data must be processed using the identical splitting rule applied to the Monte Carlo samples. Specifically, for any given event, the modulo operation $(\text{Event Number}) \pmod 5 = i_{\text{fold}}$ dictates which specific network in the ensemble ($\text{DNN}_{i}$) is exclusively used to score it. This method guarantees that the evaluation of the experimental data mirrors the independent evaluation of the respective MC test sets, thereby preserving the identically distributed assumption.

    % subsection datasets_construction (end) 

    \subsection{Kinematic Distributions}% (fold)
    \label{sub:kinematic_distributions}

        To illustrate the separation power of the input features, we present the normalized distributions of several key kinematic variables. The comparisons are divided into two categories, reflecting the two-stage strategy of the Dense-NN training: (1) separating the $W^{\pm}W^{\pm}jj$-EW signal from various background processes, and (2) distinguishing between the different polarization states of the $W^{\pm}W^{\pm}jj$ electroweak production.


        \subsubsection{Signal vs Background Discrimination}% (fold)
        \label{subs:signal_vs_background_discrimination}
            In the first stage of the DNN, the goal is to isolate the inclusive $W^{\pm}W^{\pm}jj$-EW signal from the dominant backgrounds, which include $W^{\pm}W^{\pm}jj$-QCD, $W^{\pm}Zjj$-EW, and $W^{\pm}Zjj$-QCD.

            Figure~\ref{fig:dist_bkg} shows the distributions of six features. The VBS signature is characterized by two forward jets with a large invariant mass ($m_{jj}$) and a large pseudorapidity gap ($\abs{\Delta\eta_{jj}}$), making them powerful discriminants against QCD-induced processes. Furthermore, the Zeppenfeld variable ($Z_{\ell_1}^{\star}$) quantifies the centrality of the lepton relative to the tagging jets; signal leptons tend to fall between the two forward jets. General kinematics, such as the leading jet transverse momentum ($p_{\text{T}}^{j_1}$), di-lepton invariant mass ($m_{\ell\ell}$), and missing transverse momentum ($E_{\text{T}}^{\text{miss}}$), provide additional discrimination power against the $WZ$ and QCD backgrounds.

            \begin{figure}[htbp]
                \centering
                % 第一行三張圖
                \subfloat[$m_{jj}$]{
                    \includegraphics[width=0.31\textwidth]{sr_overlay_wwzz_mix/overlay_m_jj.pdf}
                }
                \subfloat[$\abs{\Delta\eta_{jj}}$]{
                    \includegraphics[width=0.31\textwidth]{sr_overlay_wwzz_mix/overlay_dy_jj.pdf}
                }
                \subfloat[$Z_{\ell_1}^{\star}$]{
                    \includegraphics[width=0.31\textwidth]{sr_overlay_wwzz_mix/overlay_zstar_l1.pdf}
                } \\
                \subfloat[$p_{\text{T}}^{j_1}$]{
                    \includegraphics[width=0.31\textwidth]{sr_overlay_wwzz_mix/overlay_j1_pt.pdf}
                }
                \subfloat[$m_{\ell\ell}$]{
                    \includegraphics[width=0.31\textwidth]{sr_overlay_wwzz_mix/overlay_m_ll.pdf}
                }
                \subfloat[$E_{\text{T}}^{\text{miss}}$]{
                    \includegraphics[width=0.31\textwidth]{sr_overlay_wwzz_mix/overlay_met_et.pdf}
                }
                \caption{Normalized distributions of six key kinematic variables for the inclusive $W^{\pm}W^{\pm}jj$-EW signal and the $W^{\pm}W^{\pm}jj$-QCD, $W^{\pm}Zjj$-EW, and $W^{\pm}Zjj$-QCD backgrounds.}
                \label{fig:dist_bkg}
            \end{figure}

        % subsubsection signal_vs_background_discrimination (end)	

        \subsubsection{Polarization Discrimination}% (fold)
        \label{subs:polarization_discrimination}

            The second stage of the DNN focuses entirely on the $W^{\pm}W^{\pm}jj$-EW process, aiming to separate the longitudinally polarized state (LL) from the transverse states (LT and TT).

            Figure~\ref{fig:dist_pol} illustrates six variables. The $W_{\text{L}}$ bosons tend to emit leptons more centrally and with different spin correlations compared to $W_{\text{T}}$ bosons, leading to distinct shapes in the angular separations $\abs{\Delta\eta_{\ell\ell}}$ and $\Delta\phi_{\ell\ell}$, as well as the leading lepton transverse momentum ($p_{\text{T}}^{\ell_1}$). The transverse mass of the di-lepton system ($m_{\text{T}}$) and the projected transverse mass ($m_{\text{T}}^{0}$) are also sensitive to the different kinematic boundaries of the polarization states. Additionally, the azimuthal angle difference between the jets ($\Delta\phi_{jj}$) carries information about the intermediate vector boson interference in the VBS process.

            \begin{figure}[htbp]
                \centering
                % 第一行三張圖
                \subfloat[$\abs{\Delta\eta_{\ell\ell}}$]{
                    \includegraphics[width=0.31\textwidth]{sr_overlay_ew_polar/overlay_deta_ll.pdf}
                }
                \subfloat[$m_{\text{T}}$]{
                    \includegraphics[width=0.31\textwidth]{sr_overlay_ew_polar/overlay_mt_ll_met.pdf}
                }
                \subfloat[$\Delta\phi_{jj}$]{
                    \includegraphics[width=0.31\textwidth]{sr_overlay_ew_polar/overlay_dphi_jj.pdf}
                } \\
                % 第二行三張圖
                \subfloat[$p_{\text{T}}^{\ell_1}$]{
                    \includegraphics[width=0.31\textwidth]{sr_overlay_ew_polar/overlay_j1_pt.pdf}
                }
                \subfloat[$m_{\text{T}}^{0}$]{
                    \includegraphics[width=0.31\textwidth]{sr_overlay_ew_polar/overlay_mt0_ll_met.pdf}
                }
                \subfloat[$\Delta\phi_{\ell\ell}$]{
                    \includegraphics[width=0.31\textwidth]{sr_overlay_ew_polar/overlay_dphi_l2_l1.pdf}
                }
                \caption{Normalized distributions of six polarization-sensitive variables for the LL, LT, and TT polarization states of the $W^{\pm}W^{\pm}jj$-EW process.}
                \label{fig:dist_pol}
            \end{figure}

        % subsubsection polarization_discrimination (end)

    % subsection kinematic_distributions (end)

    \subsection{Neural Network Architecture}% (fold)
    \label{sub:neural_network_architecture}

        The Dense-NN architecture utilized in the analysis is presented in Figure~\ref{fig:dnn_architecture}. 

        The network begins with an input normalization layer. This layer applies the scaling to the input features first. Then, normalizing the resulting distribution to have a mean of 0 and a variance of 1.

        Following the normalization layer, the architecture relies on a core block consisting of a fully-connected (dense) layer, a Batch Normalization layer, a Swish~\cite{DBLP:journals/corr/abs-1710-05941} activation function, and a Dropout layer for regularization. This composite block is repeated $L$ times to form the deep hidden layers of the network.

        Finally, the network terminates with an output dense layer.
        \begin{figure}[htbp]
            \centering
            \begin{tikzpicture}[
                node distance=1.2cm,
                % 定義方塊樣式，文字旋轉 90 度
                box/.style={rectangle, draw=black, thick, minimum width=1.2cm, minimum height=3.5cm, align=center, fill=gray!10, font=\small},
                block/.style={rectangle, draw=black, thick, minimum width=1.0cm, minimum height=3.0cm, align=center, fill=white, font=\small},
                arrow/.style={thick, -{Stealth[scale=1.2]}}
            ]

            % 1. 輸入與常態化層
            \node (input) [box, fill=green!5] {\rotatebox{90}{Input}};
            \node (norm) [box, right=of input] {\rotatebox{90}{Normalization}};

            % 2. 隱藏層區塊 (重複單元)
            \node (dense) [block, right=of norm] {\rotatebox{90}{Dense}};
            \node (bn) [block, right=0.4cm of dense] {\rotatebox{90}{Batch Norm}};
            \node (swish) [block, right=0.4cm of bn] {\rotatebox{90}{Swish}};
            \node (dropout) [block, right=0.4cm of swish] {\rotatebox{90}{Dropout}};

            % 使用虛線框住重複區塊
            \node[draw=blue, dashed, inner xsep=0.3cm, inner ysep=0.3cm, fit=(dense) (dropout)] (hidden_block) {};
            \node[anchor=south, color=blue] at (hidden_block.north) {Repeated $L$ times};

            % 3. 輸出層
            \node (output) [box, fill=red!5, right=of hidden_block] {\rotatebox{90}{Output}};

            % 連接箭頭
            \draw [arrow] (input) -- (norm);
            \draw [arrow] (norm) -- (hidden_block.west |- dense);
            \draw [arrow] (hidden_block.east |- dense) -- (output);

            % 區塊內部連接箭頭
            \draw [arrow] (dense) -- (bn);
            \draw [arrow] (bn) -- (swish);
            \draw [arrow] (swish) -- (dropout);

            \end{tikzpicture}
            \caption{Schematic representation of the feed-forward neural network architecture. The sequence begins with an input normalization layer. The core architecture consists of $L$ repeated hidden layers, each comprising a dense layer with a constant number of neurons, followed by batch normalization, a Swish activation function, and a dropout layer for regularization. The final dense layer provides the binary classification output.}
            \label{fig:dnn_architecture}
        \end{figure}

        The Swish activation function utilized in the hidden blocks is defined as:
        \begin{equation}
            \mathrm{swish}_{\beta}(x) = \frac{x}{1 + \euler^{-\beta x}}
        \end{equation}
        where $\beta$ is a learnable parameter or a fixed constant. Unlike ReLU, Swish is smooth and non-monotonic, characteristics that have been shown to improve gradient propagation and performance in deep networks.
        
    % subsection neural_network_architecture (end)

    \subsection{Implementation and Preliminary Validation}% (fold)
    \label{sub:implementation_and_preliminary_validation}

        The neural network architecture, data pipeline, and training loop are implemented using \verb|PyTorch 2.5.1|~\cite{10.1145/3620665.3640366}. To verify the computational setup, we conducted a preliminary validation test on both the primary inclusive task $\text{DNN}_{W^{\pm}W^{\pm}}$ and the secondary polarization task $\text{DNN}_{\text{pol}}$.

        For this initial test, we utilized the complete set of input features described in Section~\ref{sub:input_features}. The dataset comprised approximately 40,000 events for the $\text{DNN}_{W^{\pm}W^{\pm}}$ task and 73,000 events for the $\text{DNN}_{\text{pol}}$ task. The models were trained for 10 epochs using the Adam optimizer with a learning rate of $10^{-3}$ and a batch size of 256. This configuration verified the 5-fold cross-validation as well as the Dense-NN forward and backward propagation.

        Figure~\ref{fig:preliminary_training_curves} illustrates the evolution of the loss and the Area Under the ROC Curve (AUC) across the initial training epochs. The quantitative performance of the models across the 5-fold ensemble is summarized in Table~\ref{tab:preliminary_auc_results}. The negligible discrepancy between the validation and test AUC scores indicates that the data splitting strategy effectively prevents data leakage and over-training, even at this early stage. 

        \begin{table}[htbp]
            \centering
            \caption{Preliminary performance of the Dense-NN models after 10 epochs of training. The values represent the mean AUC score and its standard deviation across the 5 independent folds.}
            \label{tab:preliminary_auc_results}
            \begin{tabular}{l|cc}
                \hline
                \hline
                Classification Task                   & Validation AUC    & Test AUC          \\ \hline
                $\text{DNN}_{W^{\pm}W^{\pm}}$         & $0.786 \pm 0.078$ & $0.788 \pm 0.076$ \\
                $\text{DNN}_{\text{pol}}$ (LL vs. TX) & $0.948 \pm 0.021$ & $0.947 \pm 0.024$ \\
                \hline
                \hline
            \end{tabular}
        \end{table}

        \begin{figure}[htbp]
            \centering
            \subfloat[Loss ($\text{DNN}_{W^{\pm}W^{\pm}}$)]{
                \includegraphics[width=0.45\textwidth]{loss_ew_bg.pdf}
            }
            \subfloat[AUC ($\text{DNN}_{W^{\pm}W^{\pm}}$)]{
                \includegraphics[width=0.45\textwidth]{auc_ew_bg.pdf}
            } \\
            \subfloat[Loss ($\text{DNN}_{\text{pol}}$)]{
                \includegraphics[width=0.45\textwidth]{loss_LL_TX.pdf}
            }
            \subfloat[AUC ($\text{DNN}_{\text{pol}}$)]{
                \includegraphics[width=0.45\textwidth]{auc_LL_TX.pdf}
            }
            \caption{Preliminary training and validation curves (Loss and AUC) over 10 epochs for the inclusive and polarization DNN tasks.}
            \label{fig:preliminary_training_curves}
        \end{figure}

        As anticipated for a preliminary test, the training curves indicate that the models have not yet converged. The next steps will involve generating larger training samples, increasing the number of epochs, and testing the optimized hyperparameters.

    % subsection implementation_and_preliminary_validation (end)

    \subsection{Feature Scaling, Normalization, and Full Training}% (fold)
    \label{sub:feature_scaling_and_normalization}

        In Section~\ref{sub:implementation_and_preliminary_validation}, the preliminary training results for the $\text{DNN}_{\text{pol}}$ task yielded an unreasonably high AUC (AUC $\approx 0.948$). Upon thorough investigation, it was a result of a subtle data leakage issue during the feature normalization process. Initially, the normalization procedure (calculating the mean and standard deviation) was applied to each physics process separately before merging them into a unified dataset. This encoded class-specific properties into the scaled variables, introducing a weak label leakage that allowed the network to artificially distinguish the processes.
        
        This issue has been resolved. The scaling function is now strictly fitted on the completely merged \textit{training} dataset. The derived parameters are subsequently applied to transform the training, validation, and test sets.

        With the corrected data pipeline, we executed a full-scale training utilizing the complete set of input features described in Section~\ref{sub:input_features}. The dataset size was expanded to approximately 400,000 events for the inclusive $\text{DNN}_{W^{\pm}W^{\pm}}$ task and 740,000 events for the $\text{DNN}_{\text{pol}}$ task. 

        The networks were trained using the Adam optimizer with a learning rate of $10^{-3}$ and a batch size of 256. To prevent overfitting during this extended run, an early stopping mechanism was implemented. The validation loss was monitored, and the training was terminated if no improvement was observed for 20 consecutive epochs. Table~\ref{tab:dense_nn_hyperparameter} summarizes the hyperparameter settings.

        \begin{table}[htbp]
            \centering
            \caption{Hyperparameters used in Dense-NN training.}
            \label{tab:dense_nn_hyperparameter}
            \begin{tabular}{l|l}
            \hline\hline
            Parameter                   & Value \\ \hline
            Number of hidden layers     & 4     \\
            Number of nodes per layer   & 128   \\
            Dropout rate                & 0.3   \\
            Batch size                  & 256  \\
            Learning rate               & 0.001 \\
            Early stopping patience     & 20    \\
            \hline\hline
            \end{tabular}
        \end{table}

        Figure~\ref{fig:full_training_curves} illustrates the evolution of the loss and the AUC across the training epochs. The quantitative performance of the fully trained models across the 5-fold ensemble is summarized in Table~\ref{tab:full_auc_results}. 

        \begin{table}[htbp]
            \centering
            \caption{Final performance of the Dense-NN models across the 5 independent folds after full training with early stopping. The table presents the mean AUC scores and their standard deviations. The corrected normalization strategy yields mathematically robust and realistic separation power.}
            \label{tab:full_auc_results}
            \begin{tabular}{l|cc}
                \hline
                \hline
                Classification Task                   & Validation AUC    & Test AUC          \\ \hline
                $\text{DNN}_{W^{\pm}W^{\pm}}$ (EW vs. Background) & $0.806 \pm 0.001$ & $0.806 \pm 0.001$ \\
                $\text{DNN}_{\text{pol}}$ (LL vs. TX)             & $0.839 \pm 0.001$ & $0.839 \pm 0.001$ \\
                \hline
                \hline
            \end{tabular}
        \end{table}

        \begin{figure}[htbp]
            \centering
            \subfloat[Loss ($\text{DNN}_{W^{\pm}W^{\pm}}$)]{
                \includegraphics[width=0.45\textwidth]{loss_ew_bg-full-fold0.pdf}
            }
            \subfloat[AUC ($\text{DNN}_{W^{\pm}W^{\pm}}$)]{
                \includegraphics[width=0.45\textwidth]{auc_ew_bg-full-fold0.pdf}
            } \\
            \subfloat[Loss ($\text{DNN}_{\text{pol}}$)]{
                \includegraphics[width=0.45\textwidth]{loss_LL_TX-full-fold0.pdf}
            }
            \subfloat[AUC ($\text{DNN}_{\text{pol}}$)]{
                \includegraphics[width=0.45\textwidth]{auc_LL_TX-full-fold0.pdf}
            }
            \caption{Full training and validation curves (Loss and AUC) for the inclusive and polarization Dense-NN tasks. The early stopping mechanism successfully prevents overtraining.}
            \label{fig:full_training_curves}
        \end{figure}

        The revised test AUC for the polarization task represents a much more realistic separation power. Furthermore, the small variance between the folds and the negligible discrepancy between the validation and test AUC scores confirm that the data splitting and normalization strategy effectively prevents overtraining.

    % subsection feature_scaling_and_normalization (end)

    \subsection{DNN Output Score Distributions}% (fold)
    \label{sub:dnn_output_score_distributions}

        To visually assess the discrimination power of the trained networks, we evaluate the DNN output score distributions for both the inclusive and polarization-specific classification tasks. Figure~\ref{fig:dnn_score_distributions} presents the normalized predicted probability scores evaluated on the test datasets.

        \begin{figure}[htbp]
            \centering
            \subfloat[$\text{DNN}_{W^{\pm}W^{\pm}}$ Score Distribution]{
                \includegraphics[width=0.45\textwidth]{dnn_score_dist_ew_vs_bg_binary_unstacked.pdf}
                \label{fig:score_ww}
            }
            \subfloat[$\text{DNN}_{\text{pol}}$ Score Distribution]{
                \includegraphics[width=0.45\textwidth]{dnn_score_dist_pol_ll_vs_lttt_binary_unstacked.pdf}
                \label{fig:score_pol}
            }
            \caption{Distributions of the DNN output scores for the test datasets. (a) The inclusive $\text{DNN}_{W^{\pm}W^{\pm}}$ separating the EW signal from backgrounds. (b) The $\text{DNN}_{\text{pol}}$ distinguishes the LL signal from the TX states.}
            \label{fig:dnn_score_distributions}
        \end{figure}

        In an idealized binary classification scenario, the output scores would exhibit distinct peaks at $1.0$ for the signal and $0.0$ for the background. However, as observed in Figure~\ref{fig:dnn_score_distributions}, the distributions display a profile with noticeable overlapping regions. 
        
        Upon preliminary investigation, this behavior is attributed to the incorporation of physical event weights during the training process. However, we observed a discrepancy between our current score distributions and the reference plots presented in the thesis~\cite{stange2025longitudinal}.

    % subsection dnn_output_score_distributions (end)

    \subsection{Training Weight Strategy}% (fold)
    \label{sub:weighting_strategy}

        To properly account for the different production cross-sections and the imbalanced MC dataset sizes, we evaluated three distinct sample weighting strategies for the NN training loss function:
        \begin{enumerate}
            \item \textbf{Physical Yield Weighting:} Each event is assigned a weight corresponding to its expected physical yield in the SR.
            \item \textbf{Inverse Generation Weighting:} Each event is weighted simply by the inverse of the total number of generated events for its respective process ($w \propto 1/N_{\text{gen}}$).
            \item \textbf{Hybrid Weighting:} A combination of the physical yield and inverse event count methods.
        \end{enumerate}

        The pure ``Physical Yield'' strategy reflects the true kinematic composition and imbalance among all physical processes, closely mimicking the exact conditions of real collision data. However, if used alone, it fails to account for the imbalance within the generated MC samples themselves. This is why the ``Inverse Generation'' and ``Hybrid'' approaches were introduced and evaluated.

        Furthermore, a crucial \textbf{class-balancing} step is enforced. The event weights are globally normalized such that the sum of the weights in the signal category exactly equals the sum of the weights in the background category ($\sum w_{\text{sig}} = \sum w_{\text{bkg}}$). This global scaling preserves the relative physical ratios of the internal sub-processes while preventing the neural network from trivially predicting the majority class to minimize the loss.

        The Hybrid weighting strategy, combined with global class balancing, provides the most reasonable learning strategy. Thus, we presented the training results of this strategy. Figure~\ref{fig:dnn_score_distributions_signal_background_balanced_weight} illustrates the resulting Dense-NN output evaluated on the test datasets utilizing this optimized weighting scheme. The distributions satisfy our expectation that they exhibit peaks near 1 for the signal and 0 for the background.

        \begin{figure}[htbp]
            \centering
            \subfloat[$\text{DNN}_{W^{\pm}W^{\pm}}$ Score Distribution]{
                \includegraphics[width=0.45\textwidth]{dnn_score_dist_ew_vs_bg_binary_unstacked_hybrid.pdf}
            }
            \subfloat[$\text{DNN}_{\text{pol}}$ Score Distribution]{
                \includegraphics[width=0.45\textwidth]{dnn_score_dist_pol_ll_vs_lttt_binary_unstacked_hybrid.pdf}
            }
            \caption{Distributions of the DNN output scores for the test datasets using the optimized hybrid weighting strategy. (a) The inclusive $\text{DNN}_{W^{\pm}W^{\pm}}$ separating the EW signal from backgrounds. (b) The $\text{DNN}_{\text{pol}}$ distinguishes the LL signal from the TX states.}
            \label{fig:dnn_score_distributions_signal_background_balanced_weight}
        \end{figure}

    % subsection weighting_strategy (end)

% section dense_neural_network (end)





\section{Statistical framework}% (fold)
\label{sec:statistical_framework}

    This section outlines the statistical framework utilized to extract the final physics results. The ultimate goal of this analysis is to evaluate the Discovery Significance $Z_0$ and the Upper Limit constraints.

    \subsection{Profile Likelihood Fit}% (fold)
    \label{sub:profile_likelihood_fit}
        
        First, we need to compute the profile likelihood $\mathcal{L}$, which evaluates the goodness-of-fit between the theoretical model and the observed data across the 1D histograms. For each bin $i$, the expected number of events is defined as $m_i$, and the observed number of events is denoted as $n_i$.

        The expectation $m_i$ consists of the signal and background contributions, parameterized by the signal strength $\mu$, and is modified by systematic uncertainties. These uncertainties are modeled using nuisance parameters (NPs), denoted as $\boldsymbol{\theta}$ for systematics, and $\boldsymbol{\gamma}$ for Monte Carlo (MC) statistical uncertainties:
        \begin{equation}
            m_i( \mu, \boldsymbol{\theta}, \boldsymbol{\gamma} ) = \gamma_i \left( \mu \, s_i(\boldsymbol{\theta}) + b_i(\boldsymbol{\theta}) \right),
        \end{equation}
        where $s_i$ and $b_i$ are the nominal number of predicted signal and background events in bin $i$, respectively.

        The complete likelihood function $\mathcal{L}$ is constructed as the product of Poisson probabilities for all bins, multiplied by constraint terms for the nuisance parameters:
        \begin{equation}
            \mathcal{L}(\mu, \boldsymbol{\theta}, \boldsymbol{\gamma}) = \prod_{i=1}^{N_{\text{bins}}} \text{Pois}\left( n_i | m_i \right) \mathrm{Pois}\left(\frac{1}{(\delta_i^{\text{stat}})^2} \Bigg| \frac{\gamma_i}{(\delta_i^{\text{stat}})^2} \right) \prod_{j=1}^{N_{\text{nuis}}} f_{\text{Gauss}}(\theta_j).
        \end{equation}
        Here, the Poisson probability is defined as $\mathrm{Pois}(n_i | m_i) = \frac{m_i^{n_i}}{n_i!} \euler^{-m_i}$. The auxiliary functions $f_{\text{Gauss}}(\theta_j)$ are standard normal distributions constraining the systematic NPs $\boldsymbol{\theta}$.

        \subsubsection{Test Statistic and Profile Likelihood Ratio}
        To test specific hypotheses, the profile likelihood ratio $\lambda(\mu)$ is defined as:
        \begin{equation}
            \lambda(\mu) = \frac{\mathcal{L}(\mu, \hat{\hat{\boldsymbol{\theta}}}, \hat{\hat{\boldsymbol{\gamma}}})}{\mathcal{L}(\hat{\mu}, \hat{\boldsymbol{\theta}}, \hat{\boldsymbol{\gamma}})}.
        \end{equation}
        In the denominator, $\hat{\mu}$, $\hat{\boldsymbol{\theta}}$, and $\hat{\boldsymbol{\gamma}}$ are the unconditional maximum likelihood estimators that globally maximize $\mathcal{L}$ across the entire parameter space. In the numerator, $\hat{\hat{\boldsymbol{\theta}}}$ and $\hat{\hat{\boldsymbol{\gamma}}}$ are the conditional maximum likelihood estimators that maximize $\mathcal{L}$ for a specified, fixed value of $\mu$.

        Based on $\lambda(\mu)$, the test statistic $q_\mu$ is constructed:
        \begin{equation}
            q_\mu = -2 \ln \lambda(\mu).
        \end{equation}

        \subsubsection{Discovery Significance and Upper Limits}
        The presence of a new signal is tested by rejecting the background-only hypothesis ($\mu = 0$). The corresponding test statistic is $q_0 = -2 \ln \lambda(0)$. For a sufficiently large dataset, the discovery significance $Z_0$ can be approximated as:
        \begin{equation}
            Z_0 \approx \sqrt{q_0}.
        \end{equation}

        In the absence of a significant excess, the analysis sets a 95\% Confidence Level (CL) upper limit on the signal strength. This is derived using the $\text{CL}_s$ method, which also utilizes the test statistic $q_\mu$.

    % subsection profile_likelihood_fit (end)

    \subsection{The histogram binning}% (fold)
    \label{sub:the_histogram_binning}

        In this analysis, the outputs of the Dense-NNs serve as the direct inputs for the profile likelihood fit. The outputs can be interpreted as classiﬁcation tuple $(\text{DNN}_{\text{pol}}, \text{DNN}_{W^\pm W^\pm})$ on a two-dimensional score plane:
        \begin{itemize}
            \item Y-axis ($\text{DNN}_{W^\pm W^\pm}$): The inclusive network score, which separates the pure EW $W^\pm W^\pm jj$ signal from reducible and irreducible backgrounds (e.g., $WZ$, QCD $WW$).
            \item X-axis ($\text{DNN}_{\text{pol}}$): The polarization-specific network score, which evaluates the EW events to distinguish the targeted polarization state (e.g., LL signal) from the other polarization states (e.g., TX backgrounds).
        \end{itemize}

        This study uses a rebinning algorithm to maximize the significance and reduce the computation time. This procedure slices the 2D plane into a sequence of 1D bins, requiring a delicate balance between preserving the Dense-NN output shape information and avoiding statistical fluctuations. The algorithm is listed below:
        \begin{itemize}
            \item \textbf{Region Slicing:} The Y-axis ($\text{DNN}_{W^\pm W^\pm}$) is first divided into discrete purity intervals. Each interval is then evaluated along the X-axis ($\text{DNN}_{\text{pol}}$) and flattened into consecutive 1D strips.
            \item \textbf{Statistical Stability:} To prevent statistical uncertainties, the bin edges of each histogram are merged until there are at least ten events originating from background or transverse polarized contributions in each bin.
            \item \textbf{Significance Maximization:} Adjacent bins are merged if keeping them separate would not improve the significance.
        \end{itemize}

        Table~\ref{tab:optimized_splits} is the final binning from the thesis. 
        \begin{table}[htbp]
            \centering
            \caption{The optimized splits in the $\text{DNN}_{W^{\pm}W^{\pm}}$ classification and the corresponding bin edges in polarization classification.}
            \label{tab:optimized_splits}
            \begin{tabular}{|l|c|c|}
            \hline
            Polarization & Split in $\text{DNN}_{W^{\pm}W^{\pm}}$ & Binning in polarization classification \\ \hline
            LL in $WW$-cmf & $[0.0, 0.2, 0.6, 1.0]$ & \begin{tabular}[c]{@{}c@{}}$[0.0, 0.4, 0.7, 1.0]$\\ $[0.0, 0.5, 0.8, 1.0]$\\ $[0.0, 0.4, 0.6, 0.7, 0.8, 1.0]$\end{tabular} \\ \hline
            LX in $WW$-cmf & $[0.0, 0.3, 0.7, 1.0]$ & \begin{tabular}[c]{@{}c@{}}$[0.0, 0.3, 0.7, 1.0]$\\ $[0.0, 0.3, 0.5, 0.6, 0.7, 0.8, 1.0]$\\ $[0.0, 0.15, 0.35, 0.45, 0.55, 0.7, 1.0]$\end{tabular} \\ \hline
            LL in pp-cmf   & $[0.0, 0.15, 0.5, 1.0]$ & \begin{tabular}[c]{@{}c@{}}$[0.0, 0.55, 0.8, 1.0]$\\ $[0.0, 0.7, 0.8, 1.0]$\\ $[0.0, 0.4, 0.6, 0.7, 0.8, 1.0]$\end{tabular} \\ \hline
            LX in pp-cmf   & $[0.0, 0.25, 0.6, 1.0]$ & \begin{tabular}[c]{@{}c@{}}$[0.0, 0.45, 0.6, 1.0]$\\ $[0.0, 0.25, 0.4, 0.55, 0.75, 1.0]$\\ $[0.0, 0.15, 0.3, 0.4, 0.5, 0.6, 0.7, 1.0]$\end{tabular} \\ \hline
            \end{tabular}
        \end{table}

    % subsection the_histogram_binning (end)

    \subsection{Expected Results}% (fold)
    \label{sub:expected_results}

        The fit model must be validated using an expected dataset. This is achieved by utilizing an \textbf{Asimov dataset}, which is a representative pseudo-dataset where the ``observed'' event counts in each bin are set exactly equal to the nominal theoretical predictions (signal plus background), absent any statistical fluctuations.

        Fitting the Asimov dataset serves a critical purpose: it determines the theoretical sensitivity of the analysis based on the current detector acceptance, background estimations, and the discrimination power of the DNN. This yields a baseline expected discovery significance ($Z_0$) for the targeted polarization states.

        Table~\ref{tab:expected_significance} summarizes the expected significances for the LL and LX signal states as reported in the reference thesis~\cite{stange2025longitudinal}. 

        \begin{table}[htbp]
            \centering
            \caption{Expected discovery significances for the LL and LX polarization states, evaluated using the Asimov dataset. The comparison highlights the impact of defining the polarization vectors in the $WW$ center-of-mass frame versus the partonic frame.}
            \label{tab:expected_significance}
            \begin{tabular}{lcc}
                \hline
                \hline
                Reference Frame & LL Significance ($Z_0$) & LX Significance ($Z_0$) \\
                \hline
                WW-cmf & $1.512\,\sigma$ & $4.557\,\sigma$ \\
                pp-cmf & $0.937\,\sigma$ & $3.975\,\sigma$ \\
                \hline
                \hline
            \end{tabular}
        \end{table}

        As shown in the table, defining the polarization in the $WW$ center-of-mass frame yields notably higher expected sensitivities for both the LL and LX states compared to the initial-state partonic frame.

    % subsection expected_results (end)

    \subsection{Results of the Statistical Fit}% (fold)
    \label{sub:results_of_the_statistical_fit}

        Using the profile likelihood fit framework and the unrolled 1D binning configuration described above, we evaluate the expected discovery significance $Z_0$ and the expected $95\%$ CL upper limit on the signal strength parameter $\mu$ for both the LL mode and the LX mode. The fit is performed using the Asimov dataset constructed from our own simulated Monte Carlo samples in the $WW$ center-of-mass frame.

        Table~\ref{tab:our_expected_significance} summarizes our obtained expected discovery significances and expected $95\%$ CL upper limits on the signal strength parameters, alongside the reference results from the thesis~\cite{stange2025longitudinal} for comparison.

        \begin{table}[htpb]
            \centering
            \caption{Expected discovery significances ($Z_0$) and expected $95\%$ CL upper limits on the signal strength parameters ($\mu$) for the LL and LX polarization states in the $WW$ center-of-mass frame. The reference values from the thesis are provided for comparison.}
            \label{tab:our_expected_significance}
            \begin{tabular}{lcccc}
                \hline
                \hline
                \multirow{2}{*}{Signal State} & \multicolumn{2}{c}{Expected Significance ($Z_0$)} & \multicolumn{2}{c}{Expected 95\% CL Upper Limit} \\
                \cline{2-5}
                               & Thesis & Our Result & Thesis & Our Result \\
                \hline
                LL ($\mu_{\text{LL}}$)  & $1.512\,\sigma$ & $1.600\,\sigma$ & 2.273 & 1.255 \\
                LX ($\mu_{\text{LX}}$)  & $4.557\,\sigma$ & $4.852\,\sigma$ & --- & 0.356 \\
                \hline
                \hline
            \end{tabular}
        \end{table}

        As shown in Table~\ref{tab:our_expected_significance}, despite utilizing unoptimized Dense-NNs and slightly varied input features, our simulation successfully reproduces the overall statistical trend. The expected significances are in agreement with the reference values.

        However, a discrepancy is observed in the expected $95\%$ CL upper limit for the LL state, where our result is approximately half of the reference value. This deviation directly highlights the impact of Nuisance Parameters (NPs). Our current fit considers only statistical uncertainties. In a full fit, systematic uncertainties flatten and widen the profile likelihood curve. Consequently, the absence of these systematic constraints results in a tighter upper limit. 

    % subsection results_of_the_statistical_fit (end)

% section statistical_framework (end)

\section{Some research directions}% (fold)
\label{sec:some_research_directions}

    We propose some research directions to further enhance the discovery significance and methodological robustness of the longitudinal $W$ boson scattering study.

    \subsection{Enhance the discovery significance}% (fold)
    \label{sub:enhance_the_discovery_significance}

        The current analysis relies on a baseline Dense-NN, which flattens kinematic features and may lose underlying spatial correlations. We propose to utilize advanced architectures to boost classification accuracy and then enhance the discovery significance:
        \begin{itemize}
            \item \textbf{Advanced Architectures:} Replace the Dense-NN with CNN, \textbf{ParticleNet} (Graph Neural Networks, GNNs)~\cite{Qu:2019gqs}, or \textbf{Particle Transformer} (ParT)~\cite{Qu:2022mxj} to better capture the complex VBS topologies.
            \item \textbf{Multi-Task Learning (MTL):} Instead of training separate classifiers for inclusive $WW$ signal and polarization states, implement a single MTL framework with a shared representation space and multiple classification heads. This can yield better generalization and higher separation power.
            \item \textbf{Multi-Class classification:} Instead of utilizing the step separation strategy, implement a network to distinguish LL, LT, TT, and other background classes directly.
        \end{itemize}

    % subsection enhance_the_discovery_significance (end)

    \subsection{Unsupervised / Self-supervised learning}% (fold)
    \label{sub:unsupervised_self_supervised_learning}

        Current supervised training heavily relies on MC simulations, which introduce theoretical uncertainties and potential biases when applied to measured data.
        \begin{itemize}
            \item \textbf{Weakly Supervised Learning \& Topic Modeling:} The Classification Without Labels (CWoLa) paradigm~\cite{Metodiev:2017vrx} successfully operates on two mixed data regions for binary classification. We can extend this to multi-class scenarios using Topic Modeling~\cite{Metodiev:2018ftz}. This approach treats different mixed sample regions as ``documents'' and the underlying physics processes (e.g., pure LL, LT, TT states) as ``topics,'' enabling the extraction of pure kinematic distributions directly from collision data.
            
            \item \textbf{Self-Supervised Contrastive Learning:} Implementing self-supervised frameworks, such as JetCLR~\cite{Dillon:2021gag}, allows the neural network to learn robust, physics-invariant representations without requiring explicit MC labels. By applying physical data augmentations (e.g., azimuthal rotations, longitudinal boosts, or momentum smearing), the model maps physically similar events closer together in a latent space. This latent representation can subsequently be evaluated or fine-tuned using minimal MC labels via linear probing.
            
            \item \textbf{Objective and Impact:} The ultimate goal is to exploit these data-driven techniques to construct optimal kinematic observables and isolate the polarization states with minimal dependence on pure MC labels.
        \end{itemize}

    % subsection unsupervised_self_supervised_learning (end)

    \subsection{Fully Hadronic Channel}% (fold)
    \label{sub:fully_hadronic_channel}

        While the baseline analysis focuses on the clean same-sign fully leptonic channel ($\ell^\pm\nu\ell^\pm\nu$), the fully hadronic decay mode ($WW \to qqqq$) represents a compelling yet highly challenging frontier for VBS polarization studies. To date, no experimental results for VBS exist in this channel. 

    % subsection fully_hadronic_channel (end)

% section some_research_directions (end)

\section{Fully Hadronic Channel on VBS analysis}% (fold)
\label{sec:fully_hadronic_channel_on_vbs_analysis}

    The study of Vector Boson Scattering (VBS) in the fully hadronic decay channel ($VV \to 4q$ and $ZV \to 2\nu 2q$, where $V = W, Z$) provides a critical test of the Standard Model due to its large branching fraction. In this section, we summarize the analysis status, review the literature on fully hadronic VBS, and discuss potential phenomenological research directions.

    \subsection{Summary of literature review}% (fold)
    \label{sub:summary_of_literature_review}

        While polarized vector boson scattering has been studied in leptonic channels, there are currently no experimental results for polarized VBS (such as $W_{\text{L}} W_{\text{L}}$) in the fully hadronic channel. Even for unpolarized Standard Model VBS, no public experimental observations in the fully hadronic channel have been reported to date. This is due to the lack of distinct resonant peaks and the presence of an overwhelming QCD multijet background. This absence of fully hadronic SM VBS measurements is highlighted in ATLAS conference (2024) reviews~\cite{Wang:2909208} and a recent (2026) VBS review article~\cite{Folgueras:2026}, which omit the fully hadronic final state entirely. Currently, the only detailed study of the unpolarized Standard Model VBS in the fully hadronic final state is documented in the Ph.D. thesis of Miao Hu~\cite{hu_2022}, which is discussed in detail in Section~\ref{sub:summary_of_the_hadronic_vbs_analysis}.

        In contrast to the Standard Model VBS measurements, the only public experimental results in the fully hadronic final state at the LHC are searches for heavy resonances decaying to boosted vector boson pairs ($VV \to qqqq$), such as those conducted by ATLAS~\cite{ATLAS:2019jov} and CMS~\cite{CMS:2022qyn}. These searches exploit highly boosted topologies where the decay products of each vector boson are collimated into a single large-radius jet. By employing advanced jet substructure techniques, these analyses successfully suppress the massive QCD multijet background.

        On the phenomenological side, studies have investigated the potential of the hadronic VBS final state to probe new physics. For instance, Ref.~\cite{Delgado:2019} has explored the sensitivity of the LHC to the resonance in the $WW$ hadronic channel using boosted topologies.

    % subsection summary_of_literature_review (end)

    \subsection{Summary of the Hadronic VBS Analysis}% (fold)
    \label{sub:summary_of_the_hadronic_vbs_analysis}

        The key aspects of the analysis in Ref.~\cite{hu_2022} are summarized below:
        \begin{itemize}
            \item \textbf{Event Categorization:} One of the vector bosons is required to decay in a hadronic channel only, and this vector boson can be boosted or resolved. The other vector bosons are considered in three decay modes: $Z \to \nu\nu$, boosted $Z \to c \overline{c}/ b \overline{b}$, or boosted $V \to q'q'$. Thus, $2\times 3 =6$ categories are supposed to be identified. However, two all-visible resolved regions are not considered.
            \begin{itemize}
                \item \textbf{Boosted-Invisible (B-MET):} One boosted $V \to qq$ and one invisibly decaying $Z \to \nu\nu$, characterized by high $E_{\text{T}}^{\text{miss}}$ and one AK8 jet.
                \item \textbf{Boosted-withB (B-Btag):} Two boosted $V$ bosons with at least one decaying to heavy-flavor quarks ($Z \to b\bar{b}/c\bar{c}$).
                \item \textbf{Boosted-Boosted (B-B):} Two boosted $V$ bosons decaying to light quarks, characterized by low $E_{\text{T}}^{\text{miss}}$ and two identified AK8 jets.
                \item \textbf{Resolved-Invisible (R-MET):} One resolved $V \to qq$ and one invisibly decaying $Z \to \nu\nu$, characterized by high $E_{\text{T}}^{\text{miss}}$, zero AK8 jets, and multiple AK4 jets.
            \end{itemize}
            
            \item \textbf{Results:} Using a simultaneous fit to the MVA templates across the signal and control regions, the combined analysis reports an expected significance of $3.9\,\sigma$ (specifically $3.88\,\sigma$) for the EW $VV$ production.
        \end{itemize}

    % subsection summary_of_the_hadronic_vbs_analysis (end)

    \subsection{Possible research directions}% (fold)
    \label{sub:possible_research_directions}

        In the experimental CMS analysis~\cite{hu_2022}, the two ``all-visible'' categories are discarded. This is due to trigger bottlenecks at the LHC as well as the overwhelming QCD multijet background. Both experimental searches and phenomenological VBS studies have focused almost exclusively on boosted boson topologies. Currently, there is no discussion in the literature regarding the feasibility of a fully resolved hadronic VBS final state, where all vector boson decay products are resolved into individual jets.

        We propose to investigate the feasibility of the fully resolved VBS hadronic final state. The primary limitations in this channel are the complex jet combinatorics and signal-background separation. To address these challenges, we can employ advanced machine learning architectures:
        \begin{itemize}
			\item Jet Pairing and Classification with \textsc{Spa-Net}: In the fully resolved channel ($VVjj \to 6j$), matching the six reconstructed jets to their parent particles is a severe combinatorics problem. We can utilize Symmetry Preserving Attention Networks (\textsc{Spa-Net}) to perform jet pairing and event classification. By training \textsc{Spa-Net} to learn the underlying symmetries of the VBS decay topology, we can reconstruct the parent bosons and distinguish the VBS signal from the QCD multijet background.

				Furthermore, as discussed in Ref.~\cite{Li:2024qip}, generalized \textsc{Spa-Net} models can simultaneously handle both boosted and resolved topologies, offering a unified reconstruction framework for future studies.
        \end{itemize}

    % subsection possible_research_directions (end)

    \subsection{Proposed analysis workflow for the resolved channel}% (fold)
    \label{sub:proposed_analysis_workflow_for_the_resolved_channel}

        To implement the fully resolved hadronic VBS polarization analysis, we propose the following step-by-step workflow:
        \begin{enumerate}
            \item \textbf{Event Generation and Truth Matching:} Generate Monte Carlo samples for the signal electroweak process ($pp \to WWjj \to qqqqjj$ in polarized states $W_{\text{L}} W_{\text{L}}$, $W_{\text{L}} W_{\text{T}}$, and $W_{\text{T}} W_{\text{T}}$) and the dominant QCD multijet background. A truth-matching script will match reconstructed $R=0.4$ jets to generator-level quarks to label the correct jet-parton assignments for \textsc{Spa-Net} training.
            \item \textbf{Pre-selection cuts:} Apply basic event selection cuts to identify VBS candidate events, such as requiring at least six reconstructed jets with $p_{\text{T}} > 20\,\mathrm{GeV}$ and $|\eta| < 5$. Reference the Ph.D. thesis of Miao Hu~\cite{hu_2022}.
            \item \textbf{\textsc{Spa-Net} training and inference:} Train \textsc{Spa-Net} on the truth-matched resolved samples to perform jet-parton assignment and classify electroweak signal against the QCD background. During inference, \textsc{Spa-Net} will output the most probable jet-parton pairing and event-level classification scores.
            \item \textbf{Observable reconstruction and Neural Network scores:} Using the jet pairings predicted by \textsc{Spa-Net}, we reconstruct the parent $W$ bosons. We can either construct physical decay angles or utilize the output classification scores from the neural network.
            \item \textbf{Statistical fit:} Use the resulting distributions to perform a profile likelihood fit to evaluate the expected discovery significance or the upper limit for the longitudinal polarization (or the $WW$ channel).
        \end{enumerate}

    % subsection proposed_analysis_workflow_for_the_resolved_channel (end)
    
% section fully_hadronic_channel_on_vbs_analysis (end)

\section{Particle Transformer}% (fold)
\label{sec:particle_transformer}

    As a next step to enhance the classification performance, we explore advanced neural network architectures. First, we investigate the Particle Transformer (ParT)~\cite{Qu:2022mxj}.

    To evaluate the potential of ParT in VBS polarization studies, we test two distinct input feature sets: a high-level representation based on reconstructed physics objects and a low-level representation based on detector-level constituents.

    \subsection{Input Feature Sets}% (fold)
    \label{sub:part_input_features}

        \begin{itemize}
            \item \textbf{High-Level Feature Set (6 features per object):} The inputs are constructed from the main reconstructed physics objects in the signal region: the $2$ leptons, the $2$ tagging jets, and the missing transverse energy (MET), forming a sequence of $5$ objects in total. For each object, the input features consist of:
                \begin{itemize}
                    \item $p_{\text{T}}$: Transverse momentum.
                    \item $\eta$: Pseudorapidity.
                    \item $\Delta\phi(x, \ell_1)$: Azimuthal angle difference relative to the leading lepton $\ell_1$.
                    \item One-hot encoding ($3$-dimensional vector): Identifies the object type as lepton, jet, or MET.
                \end{itemize}
                This yields a feature matrix of shape $5 \times 6$ per event.

            \item \textbf{Low-Level Feature Set (5 features per constituent):} The inputs are constructed from the detector-level tracks and calorimeter towers. To maintain a fixed sequence size for the transformer, a maximum of $128$ constituents with the highest $p_{\text{T}}$ in the event are selected. If an event has fewer constituents, NaN-padding is applied. For each constituent, the input features consist of:
                \begin{itemize}
                    \item $p_{\text{T}}$: Transverse momentum.
                    \item $\eta$: Pseudorapidity.
                    \item $\phi$: Azimuthal angle.
                    \item One-hot encoding ($2$-dimensional vector): Identifies the constituent type as track or calorimeter tower.
                \end{itemize}
                This yields a feature matrix of shape $128 \times 5$ per event.
        \end{itemize}

    % subsection part_input_features (end)

    \subsection{Training curves}% (fold)
    \label{sub:part_training_curves}

        The Particle Transformer models are trained using 5-fold cross-validation with an early stopping mechanism. Figure~\ref{fig:part_training_curves} presents the training and validation curves (Loss and AUC) for both the high-level and low-level feature training on the EW $W^\pm W^\pm$ vs. Bkg task.
        \begin{figure}[htbp]
            \centering
            \subfloat[Loss (High-Level features)]{
                \includegraphics[width=0.45\textwidth]{loss_ew_bg-ParT-high-fold0.pdf}
            }
            \subfloat[AUC (High-Level features)]{
                \includegraphics[width=0.45\textwidth]{auc_ew_bg-ParT-high-fold0.pdf}
            } \\
            \subfloat[Loss (Low-Level features)]{
                \includegraphics[width=0.45\textwidth]{loss_ew_bg-ParT-low-fold0.pdf}
            }
            \subfloat[AUC (Low-Level features)]{
                \includegraphics[width=0.45\textwidth]{auc_ew_bg-ParT-low-fold0.pdf}
            }
            \caption{Training and validation curves for the ParT trained with high-level and low-level feature sets on the EW $W^\pm W^\pm$ vs. Bkg task.}
            \label{fig:part_training_curves}
        \end{figure}

    % subsection part_training_curves (end)

    \subsection{Performance comparison}% (fold)
    \label{sub:part_performance_comparison}

        To quantitatively compare the classification performance of the ParT against the baseline Dense-NN, we evaluate the test AUC across three distinct training tasks: the inclusive classification task (EW $W^\pm W^\pm$ vs. Background) and the two polarization-specific tasks (LL vs. TX and LX vs. TT). Table~\ref{tab:mva_auc_comparison} summarizes the final test AUC results.

        \begin{table}[htbp]
            \centering
            \caption{Comparison of the final test AUC scores between the baseline Dense-NN and the ParT utilizing high-level and low-level features across the three training tasks. The values represent the mean AUC score and its standard deviation across the 5 independent folds.}
            \label{tab:mva_auc_comparison}
            \begin{tabular}{l|ccc}
                \hline
                \hline
                Classification Task & Dense-NN (Baseline) & ParT (High-Level) & ParT (Low-Level) \\
                \hline
                EW $W^\pm W^\pm$ vs. Bkg & $0.806 \pm 0.001$ & $0.779 \pm 0.001$ & $0.933 \pm 0.016$ \\
                LL vs. TX     & $0.839 \pm 0.001$ & $0.823 \pm 0.001$ & $0.819 \pm 0.001$ \\
                LX vs. TT     & $0.781 \pm 0.001$ & $0.772 \pm 0.001$ & $0.763 \pm 0.003$ \\
                \hline
                \hline
            \end{tabular}
        \end{table}

        As shown in Table~\ref{tab:mva_auc_comparison}, for the polarization tasks (LL vs. TX and LX vs. TT), the performance of the ParT is comparable to or slightly lower than the baseline Dense-NN. However, for the inclusive EW $W^\pm W^\pm$ vs. Bkg classification, the low-level ParT configuration achieves a high test AUC of $0.933 \pm 0.016$, significantly outperforming both the high-level ParT ($0.779 \pm 0.001$) and the baseline Dense-NN ($0.806 \pm 0.001$). 

        This improvement indicates that the low-level model may utilize the total count of constituent particles to distinguish the electroweak signal from the background. Figure~\ref{fig:constituent_multiplicity} presents the distributions of the number of tracks and calorimeter towers for both EW $W^\pm W^\pm$ and background events. The figure illustrates a systematic shift toward higher constituent multiplicities in background events compared to the electroweak signal.
        \begin{figure}[htbp]
            \centering
            \subfloat[Number of tracks]{
                \includegraphics[width=0.45\textwidth]{n_tracks_dist_ew_vs_bkg.pdf}
            }
            \subfloat[Number of calorimeter towers]{
                \includegraphics[width=0.45\textwidth]{n_towers_dist_ew_vs_bkg.pdf}
            }
            \caption{Distributions of the number of tracks and calorimeter towers for the electroweak $W^\pm W^\pm$ signal and background processes.}
            \label{fig:constituent_multiplicity}
        \end{figure}

        Figure~\ref{fig:constituent_multiplicity_polarization} also presents the distributions for EW $W^\pm W^\pm$ with different polarizations. The figure demonstrates similar constituent multiplicities in LL, LT, and TT processes. This shows a reason why the ParT with low-level inputs didn't outperform the high-level case, since the NN can not simply use the multiplicity to distinguish them.
        \begin{figure}[htbp]
            \centering
            \subfloat[Number of tracks]{
                \includegraphics[width=0.45\textwidth]{n_tracks_dist_polarization.pdf}
            }
            \subfloat[Number of calorimeter towers]{
                \includegraphics[width=0.45\textwidth]{n_towers_dist_polarization.pdf}
            }
            \caption{Distributions of the number of tracks and calorimeter towers for the electroweak $W^\pm W^\pm$ LL, LT, and TT processes.}
            \label{fig:constituent_multiplicity_polarization}
        \end{figure}

    % subsection part_performance_comparison (end)

    \subsection{Training Hyperparameters}% (fold)
    \label{sub:training_hyperparameters}

        We employ a lightweight configuration of the Particle Transformer model (\texttt{ParT\_Light}) to prevent overfitting on our datasets. Table~\ref{tab:part_training_hyperparameters} details the training hyperparameters and optimization settings for both the high-level and low-level feature training.
        \begin{table}[htbp]
            \centering
            \caption{Training hyperparameters utilized in the Particle Transformer training for high-level and low-level configurations.}
            \label{tab:part_training_hyperparameters}
            \begin{tabular}{l|cc}
                \hline
                \hline
                Hyperparameter & High-Level Features & Low-Level Features \\
                \hline
                Input Sequence Length & 5 & 128 \\
                Input Feature Dimension & 6 & 5 \\
                Optimizer & Adam & Adam \\
                Learning Rate & 0.001 & 0.001 \\
                Batch Size & 2048 & 512 \\
                Max Epochs & 200 & 200 \\
                Early Stopping Patience & 10 epochs & 10 epochs \\
                Weight Strategy & Hybrid & Hybrid \\
                Random Seed & 42 & 42 \\
                \hline
                \hline
            \end{tabular}
        \end{table}

        The training, validation, and testing splits follow a deterministic 5-fold cross-validation scheme matching the Dense-NN setup.

    % subsection training_hyperparameters (end)

	\subsection{Refined Low-Level Features}% (fold)
	\label{sub:refined_low_level_features}

        Since the original low-level configuration did not yield better results for the polarization separation tasks (LL vs. TX and LX vs. TT), we investigate refined low-level input feature configurations. Here, we focus on expanding the one-hot encoding representation by dividing the constituent properties into tag and type categories:
        \begin{itemize}
            \item \textbf{Tag Features (4 channels):} Identifies the parent object associations of the constituent: \texttt{Jet1}, \texttt{Jet2}, \texttt{lepton1}, or \texttt{lepton2}.
            \item \textbf{Type Features (5 channels):} Identifies the detector reconstruction type of the constituent: \texttt{Track}, \texttt{Tower}, \texttt{Electron}, \texttt{Muon}, or \texttt{others}.
        \end{itemize}
        Combining both tag and type categories yields a 9-dimensional one-hot encoding vector. Together with the 3 coordinate features ($p_{\text{T}}$, $\eta$, $\phi$), the total input feature dimension per constituent is expanded to 12.

        To identify which components provide the most significant classification improvement, we compare the training performance across different input settings:
        \begin{enumerate}
            \item The original low-level configuration (2 channels).
            \item The combined tag and type configuration (9 channels).
            \item The tag-only configuration (4 channels).
            \item The type-only configuration (5 channels).
        \end{enumerate}

        Table~\ref{tab:refined_low_level_comparison} summarizes the test AUC results for these configurations across the three training tasks.

        \begin{table}[htbp]
            \centering
            \caption{Comparison of the final test AUC scores for the ParT utilizing different low-level input configurations. The first row lists the baseline original low-level configuration (2 channels), while the subsequent rows present the 9-channel, 4-channel (Tag), and 5-channel (Type) configurations.}
            \label{tab:refined_low_level_comparison}
            \begin{tabular}{l|ccc}
                \hline
                \hline
                Input Configuration & EW $W^\pm W^\pm$ vs. Bkg & LL vs. TX         & LX vs. TT         \\ \hline
                Track \& Tower      & $0.933 \pm 0.016$        & $0.819 \pm 0.001$ & $0.763 \pm 0.003$ \\
                Tag \& Type         & $0.9433$                 & $0.8253$          & $0.7710$          \\
                Tag Only            & $0.9314$                 & $0.8242$          & $0.7692$          \\
                Type Only           & $0.9458$                 & $0.8270$          & $0.7706$          \\
                \hline
                \hline
            \end{tabular}
        \end{table}

	% subsection refined_low_level_features (end)

% section particle_transformer (end)

\bibliographystyle{ieeetr}
\bibliography{reference}


\end{document}
